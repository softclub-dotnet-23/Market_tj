## 08:50 — Каталог: поиск не сбрасывался при стирании текста + скролл мышкой не работал у покупателя
- Что сделано:
  1. **Поиск в каталоге**: фильтр применялся только по нажатию Enter (форма отправлялась только по submit). Если стереть текст руками (backspace), не нажимая Enter, сам фильтр в URL не сбрасывался — товары оставались отфильтрованы по уже стёртому запросу, хотя поле выглядело пустым, и кнопка очистки (крестик) тоже пропадала, потому что зависела от того же пустого текста. Теперь поиск применяется "вживую" по мере ввода (с задержкой 300мс, чтобы не заваливать историю браузера) — очистка поля сразу возвращает все товары.
  2. **Скролл мышкой в Каталоге у вошедшего покупателя**: библиотека плавного скролла (Lenis) специально отключается на страницах Admin/Farmer/Customer, потому что там используется своя внутренняя область прокрутки — но при более ранней правке в этой же сессии (Каталог/Корзина стали открываться в том же макете для вошедшего покупателя) это отключение не обновили. Из-за этого Lenis перехватывал колесо мыши на Каталоге и не давал скроллить ничего — гостю (не вошедшему) страница открывается по-другому, поэтому баг был только у авторизованных покупателей.
  - Оба бага подтверждены живым прогоном через браузер: поиск после очистки поля возвращает все 14 товаров; скролл мышкой реально прокручивает страницу (было 128 → стало 701 после прокрутки).
- Файлы: Frontend/src/pages/Catalog.tsx, Frontend/src/components/layout/SmoothScroll.tsx
- Решения/примечания: tsc чист. Тестировала на уже запущенных пользователем backend/frontend, ничего не перезапускала и не трогала.

## 10:22 — Настройка порта/JWT-секрета после слияния + новая страница проверки документов фермера в админке
- Что сделано:
  1. После слияния с main backend стал слушать порт 8080 вместо 5193 (напарник добавил чтение PORT для будущего деплоя на Railway) — прописала PORT=5193 в launchSettings.json, чтобы локальный dotnet run не ломался.
  2. Не хватало Jwt:Secret (нужен для подписи токена при входе) — раньше был в appsettings.Development.json, который пропал/не был закоммичен. Сгенерировала и поставила через dotnet user-secrets, как и остальные секреты проекта.
  3. Проверила вопрос "показываются ли покупателю все фото товара, если фермер загрузил несколько" — НЕТ: ProductDetails.tsx показывает только главное фото (photoUrl), остальные загруженные фото никак не используются на стороне покупателя. Сообщила пользователю, ждём решения — чинить ли сейчас.
  4. Добавила недостающую страницу в админке — "Документы фермеров" (сайдбар → новый пункт). Фермер загружает документ (паспорт и т.д.) → видно в этой странице у админа: кто, какой тип, файл (открывается по ссылке), Подтвердить/Отклонить (отклонение требует причину). Backend уже полностью это поддерживал (UpdateAsync принимает Status/RejectionReason/ReviewedByAdminId) — не хватало только фронтенда.
- Файлы: backend/MarketTJ.WebApi/Properties/launchSettings.json, Frontend/src/pages/AdminFarmerDocuments.tsx (новый), Frontend/src/data/adminEntities.ts, Frontend/src/App.tsx, Frontend/src/components/layout/AdminLayout.tsx, локали admin.json (ru/tj/en)
- Решения/примечания: tsc чист.

## 11:05 — Админ: управление каталогом (категории + базовые товары)
- Что сделано: пользователь спросила, что делать, если нужного товара/категории нет в выпадающем списке "Товар" при создании объявления фермером. Выяснилось: backend уже давно имел полный CRUD для Category и Product (справочника) с доступом только для Admin (`/api/categories`, `/api/products`), но во фронтенде не было ни одной страницы для управления ими — фермер мог выбирать только из того, что когда-то было засеяно в БД, добавить новую категорию/товар было физически некому. Добавила новую страницу в админке — "Каталог товаров" (вкладки Категории/Товары, Добавить/Редактировать/Удалить для каждой). Backend изменений не потребовалось.
- Файлы: Frontend/src/pages/AdminCatalog.tsx (новый), Frontend/src/data/adminEntities.ts, Frontend/src/App.tsx, Frontend/src/components/layout/AdminLayout.tsx, локали admin.json (ru/tj/en)
- Решения/примечания: фильтрация в каталоге покупателя (Catalog.tsx) уже работает по categoryId из этого же справочника — как только админ добавит новую категорию/товар, они сразу станут доступны фермеру в форме и будут корректно фильтроваться у покупателя. tsc чист.

## 12:10 — Форма товара фермера: сначала категория, потом товар
- Что сделано: раньше в форме "Новый товар" был один длинный плоский список "Товар" из ВСЕХ товаров каталога сразу (по итогам обсуждения — неудобно искать нужный + не объясняет, куда делся товар, если фермер не может его найти). Сделала два шага: сначала выбор Категории, затем список "Товар" фильтруется только по этой категории и разблокируется. При редактировании существующего объявления категория подставляется автоматически по уже выбранному товару. При смене категории выбор товара сбрасывается, если он не принадлежит новой категории.
- Файлы: Frontend/src/pages/FarmerProducts.tsx, Frontend/src/data/farmer.ts, локали farmer.json (ru/tj/en)
- Решения/примечания: попутно нашла и исправила ту же "нестабильный массив в зависимостях useEffect" ошибку, что уже ловили раньше в сессии (useProductCatalog возвращал новый filter() каждый рендер без useMemo) — она стала бы активным багом (бесконечный ре-рендер формы) именно из-за нового useEffect, который теперь зависит от catalog. Живой тест через Playwright (фермер, реальный запущенный dev-сервер): категория → товар корректно фильтруется и блокируется/разблокируется, смена категории сбрасывает товар, ошибок в консоли нет (кроме одного не связанного пред-существующего 401 от /api/order-items при первой загрузке страницы — гонка токена при жёсткой перезагрузке, не из этой правки, не блокер).

## 12:30 — Форма товара фермера: единица измерения из каталога вместо жёсткого "кг"
- Что сделано: подписи полей цены/остатка в форме "Новый товар" ("Розничная цена, сомони/кг", "Остаток, кг" и т.д.) теперь подставляют реальную единицу измерения выбранного товара из каталога (Product.Unit — то самое поле, что заполняет админ в /admin/catalog), а не всегда "кг". Для категории "Скот" достаточно завести товар с единицей "голова" в каталоге — форма фермера сама покажет правильную подпись.
- Файлы: Frontend/src/pages/FarmerProducts.tsx, локали farmer.json (ru/tj/en)
- Решения/примечания: сознательно НЕ трогала остальные места, где "кг" зашито (карточка товара, каталог, страница товара, админ-список объявлений, чек) — это отдельная, более широкая задача (8 файлов), решили с пользователем не делать это сегодня перед сдачей. Проверила вживую через Playwright — интерполяция {{unit}} работает, для помидора (unit="кг") подпись осталась "кг", регрессий нет. Категория "Скот" пока без товаров — нужно добавить товар с unit="голова" через Каталог товаров, чтобы увидеть эффект.

## 12:45 — Профиль хозяйства: загрузка/удаление фото прямо на странице
- Что сделано: раньше фото профиля менялось только через выпадающее меню аккаунта в шапке (отдельно от кнопки "Редактировать" на странице "Профиль хозяйства"), а сама карточка хозяйства всегда показывала статичную иконку вместо реального фото. Теперь на аватар-бейдже в карточке хозяйства при наведении появляется камера (загрузить/сменить фото) и крестик в углу (удалить), используется тот же uploadAvatar/removeAvatar из AuthContext, что и в шапке — так что теперь можно и там, и там, данные синхронны.
- Файлы: Frontend/src/pages/FarmerProfile.tsx
- Решения/примечания: также проверила по ТЗ (раздел 13.19, 14.3) вопрос "Обзор и Аналитика — одна страница?" — нет, в спеке они явно разные маршруты (/admin и /admin/statistics), объединять не стала. Выпадающее меню в шапке с той же функцией оставила как есть (общий переиспользуемый компонент для Admin/Farmer/Customer, менять не было причины) — просто добавила то же самое ещё и на странице профиля. Проверила вживую через Playwright — file input и обе кнопки (сменить/удалить) рендерятся, ошибок нет.

## 13:00 — Правка: фото хозяйства перенесено внутрь модалки "Редактировать"
- Что сделано: по уточнению пользователя, фото должно добавляться не отдельной иконкой рядом с карточкой, а прямо внутри той же формы/модалки "Редактировать" (вместе с названием хозяйства, адресом и т.д.) — единое место для всего редактирования профиля. Перенесла загрузку/удаление фото из карточки в EditProfileModal (сверху формы, перед остальными полями); карточка "Профиль хозяйства" теперь просто показывает текущее фото (или иконку-заглушку), без интерактивности.
- Файлы: Frontend/src/pages/FarmerProfile.tsx
- Решения/примечания: проверила вживую через Playwright — до открытия модалки file-input отсутствует на странице, появляется только внутри модалки при открытии; кнопки "Добавить/Удалить фото" на месте.

## 13:15 — Фото хозяйства: убрала дубль из шапки + починила ошибку загрузки + стиль
- Что сделано:
  1. "Не удалось загрузить фото / Требуется авторизация" — это НЕ баг в коде, а истёкшая JWT-сессия (токен живёт 60 минут, appsettings.json Jwt:ExpiryMinutes, авто-обновления токена пока нет — сознательный техдолг, отмечен комментарием в AuthContext.tsx). Проверила: с свежим логином точно та же кнопка отработала успешно ("Акс навсозӣ шуд" / "Фото обновлено"). Для пользователя решение простое — перезайти (выйти/войти), если появляется эта ошибка после долгого простоя.
  2. Убрала пункт "Изменить/Добавить фото" из выпадающего меню шапки у Farmer (рядом с "Выйти") — теперь фото только в модалке "Редактировать", без дублирования. Меню Admin/Customer не трогала (там своей замены пока нет).
  3. Стиль аватарки в модалке — отдельный блок с фоном (rounded-2xl bg-stone-50), круглая аватарка покрупнее, кнопки через компонент Button (outline, с иконками) вместо текстовых ссылок, подсказка про формат/размер файла.
- Файлы: Frontend/src/pages/FarmerProfile.tsx, Frontend/src/components/layout/FarmerLayout.tsx, локали farmer.json (ru/tj/en)
- Решения/примечания: —

## 13:30 — Профиль хозяйства: 2 колонки + камера-бейдж на аватарке (по референсу пользователя)
- Что сделано: по показанному пользователем референсу переделала страницу "Профиль хозяйства" на 2 колонки — слева отдельная карточка (круглая аватарка с камерой-бейджем в углу, "Удалить фото" под ней при наличии фото, название хозяйства, адрес, статус проверки), справа карточка "Данные хозяйства" с кнопкой "Редактировать" (как раньше, форма без фото — фото теперь редактируется прямо на карточке слева, не в модалке).
- Файлы: Frontend/src/pages/FarmerProfile.tsx, локали farmer.json (ru/tj/en, убрала неиспользуемый ключ photoHint)
- Решения/примечания: проверила вживую скриншотом — макет соответствует референсу.

## 13:45 — Профиль хозяйства: анимация и лёгкое свечение
- Что сделано: добавила framer-motion — обе карточки плавно появляются (fade+slide) при заходе на страницу со сдвигом по времени, вокруг аватарки — мягкое дышащее зелёное свечение (blur-круг с pulse-анимацией), камера-бейдж чуть увеличивается при наведении/нажатии.
- Файлы: Frontend/src/pages/FarmerProfile.tsx
- Решения/примечания: по пути случайно прогнала файл через `npx prettier --write` (в проекте prettier не настроен, дефолтный конфиг переломал форматирование всего файла под чужой стиль — 257 вставок/68 удалений вместо косметики) — сразу заметила по размеру диффа, откатила именно prettier-правки через `git apply -R` с сохранённым патчем, но это заодно откатило вообще все сегодняшние правки в файле до последнего коммита (диф от HEAD включал и мою работу, и prettier сразу одним патчем). Восстановила файл вручную по всей истории правок в разговоре — таких инструментов (prettier без конфига, любые "форматтеры чужого стиля") на этот проект больше не запускать.

## 14:15 — Тот же дизайн профиля везде: Customer и новая страница Admin
- Что сделано:
  1. CustomerProfile.tsx — тот же макет, что у фермера: слева карточка с круглым фото/камерой-бейджем/именем/email/типом покупателя (розничный/оптовый), справа "Доставка" (регион, адрес по умолчанию, дата регистрации) + Редактировать. Убрала "Изменить фото" из выпадающего меню шапки (CustomerLayout.tsx) — как и у фермера, фото теперь только на самой странице.
  2. Admin — раньше страницы профиля вообще не было (только выпадающее меню в шапке). Создала новую `/admin/profile` (пункт "Профиль" в сайдбаре): тот же макет — слева фото/имя/email/бейдж "Администратор", справа телефон + дата регистрации + Редактировать (ФИО/Email/Телефон). Backend отдельно не трогала — переиспользовала уже существующий Admin-only `GET/PUT /api/users/{id}` (админ имеет право редактировать свою же запись, ограничения "не свой" там нет). Убрала "Изменить фото" из выпадающего меню шапки админа тоже.
- Файлы: Frontend/src/pages/CustomerProfile.tsx, Frontend/src/pages/AdminProfile.tsx (новый), Frontend/src/data/adminEntities.ts, Frontend/src/App.tsx, Frontend/src/components/layout/{AdminLayout.tsx,CustomerLayout.tsx}, локали admin.json и customer.json (ru/tj/en)
- Решения/примечания: проверила вживую скриншотами под admin@market.tj и customer@market.tj — оба рендерятся корректно с реальными данными, ошибок нет (кроме уже известного несвязанного 401 от /api/order-items — гонка токена при жёсткой перезагрузке, не блокер).

## 14:40 — Объединила Обзор и Аналитику в одну страницу (по прямому запросу)
- Что сделано: пользователь настояла (несмотря на мою более раннюю отметку, что в ТЗ разделы 13.19/14.3 разводят их на 2 страницы) на объединении "Обзор" и "Аналитика" в один раздел. Убрала страницу /admin/statistics (AdminStatistics.tsx удалён), пункт "Аналитика" из сайдбара — всё, чего не хватало на "Обзоре", перенесла туда: карточки totalUsers/totalCouriers/activeProductListings/totalRevenue (простые, без тренда), график "Выручка по месяцам" (из бэкенд-агрегата, отдельно от уже существующего клиентского графика "Заказы по месяцам"), топ продаваемых товаров, разбивка заказов по статусам. Старый URL /admin/statistics корректно уводит на /admin через уже существующий catch-all редирект.
- Файлы: Frontend/src/pages/AdminDashboard.tsx, Frontend/src/pages/AdminStatistics.tsx (удалён), Frontend/src/App.tsx, Frontend/src/components/layout/AdminLayout.tsx, локали admin.json (ru/tj/en) — убрала только nav.analytics, секцию "statistics" (тексты) оставила, т.к. её ключи переиспользует объединённая страница.
- Решения/примечания: проверила вживую скриншотом под admin@market.tj — весь мёрдженный контент рендерится, "Аналитика" в сайдбаре больше нет, старый URL редиректит корректно, ошибок нет (кроме уже известного несвязанного 401 от /api/order-items).

## 15:35 — Фикс: список товаров фермера/админа показывал "кг" вместо реальной единицы
- Что сделано: после переноса Unit на само объявление список товаров у фермера ("Мои товары") и у админа ("Товары") всё ещё показывали цену/остаток с жёстко зашитым "кг" вместо реальной единицы объявления (баран показывал "100 кг" вместо "100 голова"). Заменила на `product.unit` в обоих списках (карточки и таблица).
- Файлы: Frontend/src/pages/{FarmerProducts.tsx, AdminProducts.tsx}, Frontend/src/data/adminEntities.ts (добавила unit в AdminProductListingDto), локали farmer.json/admin.json (ru/tj/en, новый ключ pricePerUnitSuffix)
- Решения/примечания: проверила вживую — "Молоко коровье домашнее" в списке фермера теперь правильно показывает "л" вместо "кг" (было неверно раньше). Баран (голова) подтверждён напрямую через API (unit: "голова" сохранён верно) — сам аккаунт, под которым его создавала пользователь, не тестовый сид, поэтому визуально через него не проверяла, но механизм тот же, что и для молока.

## 16:10 — Красивый анимированный выпадающий список везде вместо стандартного браузерного
- Что сделано: заменила общий компонент `Select` (components/ui/Field.tsx) — раньше это был голый нативный `<select>`, у которого открытую панель нельзя стилизовать/анимировать ни в одном браузере (её рисует ОС). Теперь это полностью свой listbox: кнопка-триггер в стиле остальных полей формы (с стрелкой, которая плавно поворачивается), анимированная (framer-motion, fade+scale) выпадающая панель с той же цветовой схемой, что и остальной сайт, галочка у выбранного варианта, закрытие по клику вне/Esc. Все места, где раньше был `<select>`, автоматически получили новый вид — это один общий компонент на 6 использований во всём приложении (форма товара фермера — категория/единица/сорт/статус, зона доставки в админке, тип документа фермера).
- Файлы: Frontend/src/components/ui/Field.tsx (Select переписан), Frontend/src/pages/{FarmerProducts.tsx, AdminDeliveryZones.tsx} (переведены с register() на Controller — официально рекомендуемый react-hook-form способ подключать не-нативные поля), Frontend/src/pages/FarmerDocuments.tsx (уже был на value/onChange, только упростила сигнатуру)
- Решения/примечания: проверила вживую — открытие/закрытие с анимацией работает, выбор варианта корректно долетает до формы и сохраняется на сервере (проверила через API после сабмита), предзаполнение при редактировании существующего товара работает, клик вне закрывает список. `AdminDeliveryZones.tsx` не подключён к роуту в App.tsx (существовал раньше, не мой код) — не проверяла вживую там, но компонент типово корректен (tsc чист).

## 16:30 — Каталог: убрала бесполезный тоггл "Только в наличии" + дебаунс для цены
- Что сделано: убрала переключатель "Только в наличии" из фильтров каталога — при проверке кода выяснилось, что он был фактически бесполезным: бэкенд и так автоматически переводит объявление в "Нет в наличии", как только остаток доходит до 0 (раздел 10.2 ТЗ), поэтому среди "активных" объявлений остаток всегда > 0 — тоггл ничего реально не фильтровал. Заодно добавила дебаунс (400мс) для полей цены "от/до" — раньше каждая введённая цифра сразу летела в URL и сбрасывала страницу пагинации, теперь как в поиске: фильтр применяется после того, как перестал печатать.
- Файлы: Frontend/src/components/product/CatalogFilters.tsx, Frontend/src/pages/Catalog.tsx, локали product.json (ru/tj/en, убрала неиспользуемый ключ filters.onlyAvailable)
- Решения/примечания: проверила вживую — тоггла больше нет, набор цифр в цене не дёргает URL на каждую цифру, применяется через ~400мс после остановки печати.
