# Progress — 2026-07-19 (Frontend)

## 00:05 — Перенос готового фронтенда из тестового проекта в репозиторий
- Что сделано: Перенёс готовый React+Vite фронтенд из тестового проекта `~/Desktop/market/Frontend` в `Frontend/` этого репозитория (в корень, без подпапки). Удалил лишний мусорный объект `Frontend/Front`. Выполнил `npm install` (60 пакетов, 0 уязвимостей). Проверил сборку: `npm run build` (включая `tsc -b` typecheck) проходит успешно.
- Файлы:
  - Удалено: `Frontend/Front` (это оказался не пустая папка, а мусорный файл 16 байт с текстом `iohiuhiuuhuhuhuu`).
  - Скопировано в `Frontend/`: `src/` (146 файлов — components/layout, components/sections, components/product, components/ui, pages, context, data, types, lib, assets), `public/`, `index.html`, `package.json`, `package-lock.json`, `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `vite.config.ts`, `.gitignore`, `.oxlintrc.json`, `README.md`.
  - НЕ копировались: `node_modules`, `dist` (по требованию). Сгенерированный при проверке `dist/` удалён.
- Решения/примечания:
  - **Источник уточнён у пользователя.** Путь из задания `~/Desktop/marketj/Frontend` не существует; было два кандидата (`~/Desktop/market/` и `~/Desktop/Market.tj/`). Пользователь подтвердил `~/Desktop/market/Frontend`.
  - **`npm run dev` НЕ запускается — но это НЕ дефект переноса.** Dev-сервер стартует (VITE ready), но затем dev-оптимизатор зависимостей падает с нативной ошибкой Rust: `memory allocation ... failed` / Windows `os error 473` (ERROR_WORKING_SET_QUOTA) при загрузке множества мелких иконок `lucide-react`. Это баг связки **Vite 8 + Rolldown** на данной Windows-машине. Тот же самый краш воспроизводится в **исходном** тестовом проекте (`~/Desktop/market/Frontend` с его собственными node_modules) — значит проблема окружения/тулинга, а не перенесённого кода. `npm run build` и typecheck при этом проходят полностью. Возможное решение (не применялось без согласования): `optimizeDeps: { exclude: ['lucide-react'] }` в `vite.config.ts`.
  - **Захардкоженный текст (для следующего шага — i18n).** Весь UI-текст сейчас захардкожен на русском. Найдено: **40 компонентов/страниц (.tsx)** содержат хардкод русского текста — pages: 8, components/layout: 7, components/sections: 11, components/product: 6, components/ui: 6, context: 2. Плюс **10 файлов .ts** с хардкодом (8 файлов данных `src/data/*.ts` + `types/index.ts` (единицы «кг/шт/пучок/литр») + `lib/utils.ts` (названия месяцев, «сегодня/вчера»)). Итого **50 файлов** под вынос в переводы. По `docs/Frontend.md`: таджикский — основной язык, русский — дополнительный.

## 00:40 — Dev-сервер: попытка фикса `optimizeDeps.exclude` откачена
- Что сделано: Пробовал добавить `optimizeDeps: { exclude: ['lucide-react'] }` в `vite.config.ts` (Phase 1 Step 2). Фикс убрал краш Rolldown-оптимизатора, но вызвал **белый экран**: без пре-бандлинга браузер грузит ~1500 icon-модулей lucide-react по одному → водопад запросов. Откатил конфиг к оригиналу.
- Файлы: `Frontend/vite.config.ts` (изменён и возвращён к исходному состоянию — в git без изменений).
- Решения/примечания: **Переоценка диагноза.** Краш dev-оптимизатора (`os error 473` / `memory allocation ... failed`) воспроизводился только внутри песочницы ассистента (лимит working-set на порождаемые процессы — на 170 КБ падает при 2.4 ГБ свободной RAM; там же ранее падал `git`). В **реальном терминале пользователя** dev-сервер стартует нормально. Вывод: конфиг НЕ надо менять, коммита по шагу 2 нет. Пользователю: `Ctrl+C` → `Remove-Item -Recurse -Force node_modules\.vite` → `npm run dev` → `Ctrl+Shift+R`.

## 00:55 — Разбор соответствия фронт ↔ бэкенд + стратегия
- Что сделано: Сравнил модель фронта (`src/types/index.ts`, `src/data/*.ts`) с сущностями бэкенда (`backend/MarketTJ.Domain/Entities`, только чтение, ничего не менял).
- Файлы: — (анализ, без изменений кода).
- Решения/примечания:
  - Фронт **не интегрирован с API** вообще (нет `axios`/`fetch`/`VITE_`/`/api/`) — всё на моках из `src/data/`.
  - Структурные расхождения (для будущего слоя маппинга): фронтовый плоский `Product` = бэковый `Product` + `ProductListing`; фронтовый `Review.productId` vs бэковый `Review`(`OrderId`+`FarmerId`, отзыв на фермера); `Category` icon/photoKey/slug vs бэковый `ImageUrl`; маркетинговые поля `Farmer` (story, responseRate, tags, yearsActive) без аналога в БД. Роли совпадают (Admin/Farmer/Customer/Courier).
  - «Лишнее»: маркетинговые сущности/секции без аналога в MVP (`Testimonial`, `FaqItem`, Newsletter/PlatformStats/WhyChooseUs/DeliveryProcess/CTASection/BestFarmers/FreshHarvest) и витринные поля (oldPrice, badges, specifications, viewCount, orderCount, helpfulCount, verifiedPurchase, farmerReply). Не удаляем.
  - **Чего не хватает** vs `docs/Frontend.md`: есть только публичная витрина (частично); отсутствуют Cart/Checkout/Orders/Profile и панели Farmer/Admin/Courier.
  - **Согласованная стратегия (решение пользователя):** сейчас фронт остаётся на моках как есть; подгонку под бэкенд (интеграция + маппинг + недостающие панели) делаем ПОСЛЕ полного завершения бэкенда. Ближайший независимый от бэка шаг — i18n (таджикский основной). Типы/секции пока не трогаем.

## 01:20 — i18n: инфраструктура + пилот (Header, Footer, категории)
- Что сделано: Установлены `i18next`, `react-i18next`, `i18next-browser-languagedetector`. Создан конфиг `src/lib/i18n.ts` — namespace-файлы автоматически подключаются через `import.meta.glob("../locales/*/*.json")`, добавлять новый файл перевода можно без правки конфига. Язык по умолчанию — **tj** (таджикский), без определения по языку браузера (только явный выбор пользователя, сохранённый в `localStorage`, переопределяет tj). Добавлен `LanguageSwitcher` (кнопка ТҶ/РУ рядом с `ThemeToggle`) — вшит в `Header`. Переведены (ru+tj) и вынесены в JSON: Header, Footer, MegaMenu, MobileMenu, PopularCategories, CategoryCard, CatalogFilters — 8 компонентов полностью. Мок-данные: `src/data/categories.ts` переведён на паттерн `useCategories()`.
- Файлы:
  - Создано: `src/lib/i18n.ts`, `src/components/ui/LanguageSwitcher.tsx`, `src/locales/{ru,tj}/{common,layout,sections,product,data}.json` (10 файлов).
  - Изменено: `src/main.tsx`, `src/data/categories.ts`, layout/section/product-компоненты, `src/pages/{Catalog,ProductDetails}.tsx` (замена lookup-функций на хук).
  - Удалено: экспорты `categories`/`getCategoryById`/`getCategoryBySlug` — заменены на `useCategories()`.
- Решения/примечания:
  - **Архитектура данных:** «базовые нетекстовые поля в `.ts` + текстовые поля через `t()` внутри React-хука `useXxx()`».
  - **Почему lookup-функции стали хуком:** `Catalog.tsx` вызывал их внутри `.map()` — прямой хук нельзя (Rules of Hooks); решение — хук на верхнем уровне + `.find()` внутри.
  - **Таджикские переводы** сделаны мной, требуют проверки носителем перед продакшеном.
  - Проверено: `npm run build` + `npm run lint` — чисто (4 pre-existing предупреждения).

## 02:00–04:30 — i18n: завершение (все оставшиеся ~44 файла, коммиты Phase 1 Step 4–11)
- Что сделано: Полностью завершён вынос захардкоженного текста в переводы (ru+tj) по всему фронтенду. По частям, с проверкой build+lint и коммитом после каждой:
  - **Step 4**: MiniCart, AuthPanel, PageLoader + все 11 секций главной. `lib/utils.ts`: даты/валюта через `i18n.t()` + локаль по языку. Словарь `product:units`.
  - **Step 5**: продуктовые компоненты (ProductCard, ReviewsSection, FarmerCard, FarmerProfileCard).
  - **Step 6**: UI-примитивы (ThemeToggle, Pagination, Carousel, Breadcrumbs, Dropdown, Modal).
  - **Step 7**: все страницы (About, Contact, Forbidden, NotFound, Login, Register, Catalog, ProductDetails) + валидация форм.
  - **Step 8**: CartContext, FavoritesContext (toast-уведомления).
  - **Step 9**: мок-данные farmers/site/testimonials/faq/about/reviews.
  - **Step 10**: `products.ts` — 45 товаров.
  - **Step 11**: финальная проверка — полный паритет ключей ru/tj (355+ ключей в data.json).
- Файлы: коммиты `Phase 1 Step 4` (`4be3de5`) … `Phase 1 Step 11` (`7651392`).
- Решения/примечания:
  - **Правило «стабильный ключ vs переводимый текст»:** значения, участвующие в фильтрации (`unit`, `region`, `district`, FAQ-`groupKey`), остались нетронутыми как внутренние ключи; перевод для показа — через словари `product:units` / `data:regionLabels` / `data:districtLabels` / `data:faqGroups`.
  - **Найден и исправлен баг в `FAQSection.tsx`**: выбранная вкладка «слетала» бы при смене языка (сравнение по переведённому тексту) — переделано на стабильный `groupKey`.
  - **Хуки нельзя в `.map()`/на уровне модуля** — `FeaturedProducts`/`FreshHarvest` считали выборки вне компонента, перенесено внутрь; хук данных вызывается один раз, дальше `.find()`/`.filter()`.
  - **`reviews.ts`: убран Map-кэш** (замораживал язык комментариев); функция детерминирована по seed, пересчёт безопасен.
  - Все ~450+ таджикских строк — мной, нужна проверка носителем перед продакшеном.

## 05:00 — Красивый переключатель языка (dropdown с флагами)
- Что сделано: По макету пользователя переписал `LanguageSwitcher` с простой кнопки-переключателя (ТҶ↔РУ по клику) на **выпадающее меню**: триггер = глобус + текущий код языка (ТҶ/РУ) + шеврон; выпадающий список = флаг (кружок) + родное название языка + галочка у активного. Языки — только **Тоҷикӣ (основной) и Русский** (English НЕ добавлял, по требованию). Клик вне/Escape закрывают меню; при смене языка обновляется `document.documentElement.lang`.
- Файлы: `src/components/ui/LanguageSwitcher.tsx` (полностью переписан).
- Решения/примечания:
  - **Флаги — inline SVG, а не emoji.** На Windows флаг-эмодзи (🇹🇯/🇷🇺) не рендерятся — показываются буквами «TJ»/«RU». Поэтому нарисовал флаги SVG: РФ (бело-сине-красный триколор), Таджикистан (красно-бело-зелёный + золотая корона и 3 звезды по центру), в круглой обрезке (`rounded-full overflow-hidden`).
  - **Цвета под дизайн-систему сайта, а не под индиго с макета.** Макет был в фиолетово-синих тонах, но весь сайт — в зелёной палитре (grove) + stone-нейтрали + тёмная тема. Чтобы переключатель не выбивался из шапки, использовал те же токены, что у `ThemeToggle`/`Dropdown` (глобус grove-600, активный пункт grove-50/grove-950, hover stone). Структура (глобус + дропдаун + круглые флаги + галочка) — как на макете. Если нужен именно индиго с макета — поменяю за минуту.
  - **Родные названия языков не переводятся** (`Тоҷикӣ`/`Русский` — константы): язык всегда подписывается на самом себе.
  - Анимация появления — framer-motion, как у остальных дропдаунов (`Dropdown.tsx`/`MiniCart.tsx`).
  - Проверено: `npm run build` — чисто; `npm run lint` — по новому файлу 0 замечаний.
  - ⚠️ Визуально в браузере проверял пользователь (dev-сервер у него запущен) — из песочницы ассистента dev не поднимается (см. запись 00:40).
