## 21:00 — Публичный сайт переведён с моков на реальные данные (каталог, категории, фермеры, чекаут)

- Что сделано: Пользователь явно попросила перевести весь публичный сайт на реальные данные ("да, согласна, хочу уже работать по-настоящему") и сделать это максимально красиво и чётко, обратив внимание и на бэкенд. Это самая крупная правка за сессию — затронуты каталог, категории, фермеры, главная страница и чекаут разом.

  **1. Архитектура — общий кэш вместо переписывания каждой страницы.** Вместо того чтобы вручную переводить на async каждый из ~15 файлов, читающих `useProducts()`/`useFarmers()`/`useCategories()` (Catalog, ProductDetails, Home + 6 секций, ProductCard, FarmerCard, FarmerProfileCard, MiniCart, Checkout и т.д.), собран один новый файл `data/catalogStore.ts` — грузит `/api/categories`, `/api/products`, `/api/product-listings`, `/api/product-images`, `/api/farmer-profiles`, `/api/reviews`, `/api/order-items` один раз, кэширует и раздаёт через простой pub/sub. `data/products.ts`, `data/farmers.ts`, `data/categories.ts` стали тонкими обёртками с теми же именами экспортов (`useProducts`, `useFarmers`, `useCategories`, `getProductById`) — ни один потребитель не пришлось переписывать под async/loading, только под честные реальные поля.

  **2. Честно — не выдумывая недостающие данные.** По ходу выяснилось, что у реальных данных нет части декоративных полей мока — не стала их подделывать:
  - `orderCount`/бейдж "Хит продаж" — реально только для вошедших (`/api/order-items` требует `[Authorize]`, гостю недоступно); для гостя честно 0, бейдж просто не показывается.
  - "Выполненных заказов" на главной (было фейковых 32 400) — по той же причине (`/api/orders` тоже приватный) заменено на реальный, публично доступный сигнал: количество отзывов.
  - `viewCount` ("просмотров") — вообще убран с карточки товара, реального трекинга просмотров в проекте нет.
  - Бейджи "Скидка"/"Органика" — убраны совсем (нет `oldPrice`/сертификации в реальных данных); "Новинка" — теперь честно из `createdAt` (объявление младше 14 дней), "Премиум" — из реального `QualityGrade`.
  - Рейтинг товара технически стал рейтингом **фермера** — в базе `Review` привязан к `FarmerId`, не к конкретному товару (раздел 8.13 ТЗ), так что "отзывы о товаре" на странице товара — это реальные отзывы о фермере, который его продаёт.
  - `ownerName` (имя владельца) убран из публичных карточек фермера — `UserController` (единственное место с `User.FullName`) доступен только Admin, публично это не отдаётся; используется `farmName`.
  - Фото фермеров — карточки (`FarmerCard`) больше не тянут случайную фото-заглушку по несуществующему id, а показывают `Avatar` (инициалы или реальный `User.AvatarUrl`, если фермер его загрузил — см. бэкенд-правку).
  - Фото товаров — у только что созданных реальных `ProductListing` пока нет своих фото (кроме пары, которые пользователь уже загрузила через "Мои товары"); для остальных временно показывается похожая по типу товара картинка из уже существующей фото-библиотеки (по названию базового `Product` — "Помидор"→фото помидоров и т.д.), а не случайная. Как только фермер грузит своё фото — оно подставляется вместо заглушки автоматически.
  - Регион/район — реальные строки от фермера (свободный текст, не фиксированный список), поэтому фильтр региона в каталоге стал динамическим (показывает только реально встречающиеся значения), а не жёстко зашитым списком из 5 областей.

  **3. Чекаут наконец реальный.** Раз каталог теперь на реальных `ProductListingId`, `Checkout.tsx` больше не имитирует оформление — реально создаёт `Order`+`OrderItem` через бэкенд (группируя корзину по фермеру, если товары от разных хозяйств — у `Order` один `FarmerId`). Оформление теперь требует входа как покупатель (раньше не требовало) — если гость, честная карточка "Войдите, чтобы оформить заказ"; если вошёл без профиля покупателя (например, как Farmer/Admin) — отдельное honest-сообщение.

  **4. Мелкий бонус-фикс.** По пути нашла и поправила случайную мелочь: иконка "Всего курьеров" в статистике админа была платёжной картой — заменена на велосипед.
- Файлы: Frontend/src/data/catalogStore.ts (новый), Frontend/src/data/{products,farmers,categories,reviews}.ts (переписаны как обёртки), Frontend/src/data/{admin,customer,site}.ts, Frontend/src/types/index.ts, Frontend/src/pages/{Catalog,ProductDetails,Checkout}.tsx, Frontend/src/components/product/{ProductCard,FarmerCard,FarmerProfileCard,CatalogFilters,ProductGallery,ReviewsSection}.tsx, Frontend/src/components/sections/{Hero,FreshHarvest}.tsx, Frontend/src/components/layout/MiniCart.tsx, Frontend/src/components/ui/PhotoTile.tsx, Frontend/src/locales/{ru,tj}/{pages,sections,data,product}.json
- Решения/примечания: Полный сквозной прогон через curl на реально поднятом бэкенде (сама и подняла — с разрешения пользователя, `dotnet run` в фоне, потом оставила работать, перезапускать не нужно): все эндпоинты каталога отдают ожидаемые поля (включая `avatarUrl` у фермера — уже реально `null`/заполнено), создан настоящий `Order`+`OrderItem` тем же payload'ом, что строит `Checkout.tsx`, подтверждён через `GET` и удалён (сид не тронут). `tsc -b --noEmit`, `oxlint`, `vite build` — чисто (та же единственная предсуществующая ошибка в `AdminSettings.tsx`, не моя). `AdminDashboard.tsx` ("Обзор", мок-версия) сознательно НЕ переведён на реальные данные и явно отвязан от `useFarmers()`/`useCategories()` (раньше косвенно зависел от них через `data/admin.ts` — при переходе тех на реальные данные он бы либо упал, либо остался с пустыми виджетами; теперь у него свои автономные заглушки с теми же числами, что были). Живой браузерный прогон не делала (нет браузерного инструмента в этой среде) — стоит открыть 5173 и посмотреть каталог/карточку товара/чекаут вживую, особенно с учётом того, что реальных данных сейчас немного (2 фермера, ~16 товаров).

## 23:20 — Уведомление фермеру о новом заказе, реальный бейдж "Заказы", и статус заказа теперь можно менять из панели фермера

- Что сделано: Пользователь оформила тестовый заказ и, зайдя в панель фермера, попросила три вещи: (1) чтобы фермеру приходило настоящее уведомление о новом заказе, (2) чтобы в сайдбаре у пункта "Заказы" была реальная цифра-бейдж (счёт по строкам `Order`, а не по уникальным покупателям — отдельно уточнила, что один и тот же покупатель, оформивший 2 отдельных заказа, должен давать "2"), и следом, ещё до того как я успела реализовать первое, добавила: сделать дизайн статуса заказа красивее и дать фермеру возможность самому принимать/готовить заказ, а дальше пусть ведёт админ.

  **1. Уведомление.** `Farmer.userId` (User.Id владельца хозяйства) протянут через `catalogStore.ts` из уже загружаемого `RawFarmerProfile.userId` в публичный тип `Farmer`. В `Checkout.tsx` при сборке заказа по каждому фермеру теперь резолвится `farmerUserId` через `useFarmers()`, и `submitCustomerOrder` (data/customer.ts) после успешного создания Order+OrderItem отправляет `POST /notifications` с `{userId: farmerUserId, title: "Новый заказ", message: "У вас новый заказ ..."}`. Уведомление — не критичная часть потока: если оно не отправится, ошибка не пробрасывается наружу и заказ всё равно считается оформленным.

  **2. Бейдж "Заказы".** `FarmerLayout.tsx` теперь дополнительно вызывает `useFarmerProfile()` + `useFarmerOrders(profile?.id)` и показывает бейдж с `orders.length` у пункта "Заказы" — тот же паттерн, что уже был у `AdminLayout.tsx`. Поскольку `useFarmerOrders` считает заказы построчно (фильтр по `farmerId`, без дедупликации по покупателю), два отдельных заказа одного человека дают "2", как и просила пользователя.

  **3. Статус заказа — дизайн и права фермера.** Раньше статус заказа менялся через голый браузерный `<select>` (некрасиво, как на скриншоте пользователя) и был доступен только в Admin-панели; у фермера статус был просто нередактируемым бейджем. Сделано:
  - Новый компонент `components/ui/StatusMenu.tsx` — компактная цветная "таблетка"-триггер с анимированным (framer-motion) выпадающим списком опций, каждая опция тоже цветная таблетка с галочкой у текущей; если опций для выбора нет (кроме текущей) — таблетка автоматически блокируется (иконка замка вместо шеврона, `title` с подсказкой).
  - Общие константы статуса (`OrderStatus`, цвета/классы, ключи для перевода, полный список статусов) вынесены в `lib/orderStatus.ts` — раньше три копии одного и того же были размазаны по `AdminOrders.tsx`/`FarmerOrders.tsx` (плюс ещё по отдельному `OrderStatus` в `data/adminEntities.ts`/`data/farmer.ts`/`data/customer.ts`, эти оставлены как есть, чтобы не трогать типизацию DTO лишний раз).
  - Там же — `getFarmerNextStatuses(current)`: бизнes-правило, что именно фермер имеет право переключить. Ожидает → Принят/Отклонён → (Принят) → Готовится → (Готовится) → Готов к выдаче. С "Готов к выдаче" и дальше (назначение курьера, сборка, доставка, завершение) — уже не фермерская зона, таблетка становится заблокированной с подсказкой "Дальше заказ обрабатывает администратор".
  - `AdminOrders.tsx` переведён на `StatusMenu` с полным списком статусов (админ по-прежнему может выставить любой статус, кроме случая "Завершён" — терминальный, блокируется, как и раньше через `disabled`).
  - `FarmerOrders.tsx`: `FarmerOrderDto` (data/farmer.ts) расширен полями `customerComment/subtotal/deliveryPrice/acceptedAt/completedAt/cancelledAt` (были нужны для полного PUT-тела — раньше DTO их не включал, потому что страница была read-only); добавлена `updateFarmerOrderStatus()` (тот же PUT `/orders/{id}`, что и у админа, `OrderController` не ограничивает роль — `[Authorize]` без Role). Страница получила `refreshKey`/`busyId` и `handleStatusChange`, `useFarmerOrders` научился принимать `refreshKey` для повторной загрузки после смены статуса.
- Файлы: Frontend/src/types/index.ts, Frontend/src/data/catalogStore.ts, Frontend/src/data/customer.ts, Frontend/src/data/farmer.ts, Frontend/src/pages/Checkout.tsx, Frontend/src/components/layout/FarmerLayout.tsx, Frontend/src/lib/orderStatus.ts (новый), Frontend/src/components/ui/StatusMenu.tsx (новый), Frontend/src/pages/AdminOrders.tsx, Frontend/src/pages/FarmerOrders.tsx, Frontend/src/locales/{ru,tj}/farmer.json
- Решения/примечания: `tsc -b --noEmit`, `oxlint`, `vite build` — чисто (та же единственная предсуществующая ошибка в `AdminSettings.tsx`, не моя, и те же предсуществующие oxlint-warnings). Живой браузерный прогон и повторный curl-смок-тест не делала в рамках этого захода (нет браузерного инструмента; бэкенд для curl-проверки не поднимался повторно) — стоит открыть панель фермера на реальном заказе и попробовать переключить статус Ожидает → Принят → Готовится → Готов к выдаче, проверить что бейдж в сайдбаре и уведомление действительно появляются после нового оформления заказа.

## 23:45 — Бейдж "Заказы" у фермера теперь считает только необработанные заказы (и обновляется сразу после смены статуса)

- Что сделано: После предыдущей правки пользователь уточнила: если фермер принял заказ, бейдж больше не должен показывать по нему цифру. Раньше бейдж в `FarmerLayout.tsx` считал `orders.length` — то есть вообще все заказы фермера независимо от статуса (тот же приём, что и у Admin), поэтому цифра никогда бы не уменьшалась. Поменяла формулу на подсчёт только заказов со статусом `Pending` (`OrderStatus.Pending`) — как только фермер переводит заказ в "Принят"/"Отклонён", он выпадает из бейджа, даже если дальше ещё готовится или едет.

  Отдельная проблема, которую пришлось решить попутно: сайдбар (`FarmerLayout`) и страница заказов (`FarmerOrders`) — два разных компонента с двумя независимыми вызовами `useFarmerOrders`, каждый со своим локальным состоянием. Без моста между ними смена статуса на странице заказов не отразилась бы на бейдже в сайдбаре, пока не перезагрузить всё приложение (`FarmerLayout` не размонтируется при переходах между `/farmer/*`). Добавила лёгкий module-level pub/sub в `data/farmer.ts` (`notifyFarmerOrdersChanged()` + внутренняя подписка в `useFarmerOrders`) — по образцу того, что уже есть в `catalogStore.ts`, но без кэша данных, только сигнал "нужно перезапросить". `FarmerOrders.tsx` вызывает `notifyFarmerOrdersChanged()` сразу после успешного `updateFarmerOrderStatus` — так что бейдж в сайдбаре обновляется мгновенно, без перезагрузки страницы.
- Файлы: Frontend/src/components/layout/FarmerLayout.tsx, Frontend/src/data/farmer.ts, Frontend/src/pages/FarmerOrders.tsx
- Решения/примечания: Уведомление о непрочитанном (колокольчик) уже и раньше корректно уменьшалось при прочтении через `markNotificationRead`/`isRead` — этой части правка не касалась, only бейдж "Заказы". `tsc -b --noEmit` и `vite build` — чисто (та же предсуществующая ошибка в `AdminSettings.tsx`). Admin-бейдж "Заказы" сознательно не трогала — там логика "все заказы в очереди", а не "новые/непринятые", это осмысленно разные вещи для разных ролей.

## 00:20 — Покупателю: уведомление "заказ доставлен/завершён" + возможность оставить отзыв, статус заказа красиво показывается

- Что сделано: Пользователь попросила, чтобы после того как статус заказа станет "Доставлен" или "Завершён", покупателю приходило уведомление, и чтобы он мог оставить отзыв — плюс красиво показать статус заказа в его личном кабинете (раньше там был обычный некрасивый цветной `<span>`, как и везде до редизайна статусов на прошлых шагах). Возможности оставить отзыв у покупателя в проекте не было вообще — реализовала с нуля.

  **1. Уведомление.** Только Admin реально может выставить статус Доставлен/Завершён (у фермера цепочка останавливается на "Готов к выдаче" — см. `getFarmerNextStatuses`, курьерского личного кабинета в проекте нет, это по-прежнему в `AdminOrders.tsx`). Добавила `notifyCustomerAboutOrderStatus(order, status)` в `data/adminEntities.ts` — резолвит `CustomerProfile.Id → User.Id` через `/customer-profiles` (та же нестыковка типов Id, что уже решалась для фермера) и шлёт `POST /notifications` с разным текстом: "Заказ доставлен" при Delivered, "Заказ завершён" + приглашение оценить при Completed. Вызывается из `AdminOrders.tsx` после успешного `updateOrderStatus`, не блокирует основной поток (ошибка глушится, как и уведомление фермеру раньше).

  **2. Отзыв от покупателя.** Бэкенд (`ReviewService.CreateAsync`, раздел 10.6 ТЗ) разрешает отзыв только на **свой** заказ и только когда `Order.Status == Completed` (не Delivered — специально проверила `ReviewService.cs`, честно ограничила фронт этим же правилом, а не тем, что попросили буквально "доставлено или завершено"). Добавлены `CustomerReviewDto`, `useCustomerReviewedOrderIds()` (чтобы не показывать кнопку повторно — по одному заказу можно оставить только один отзыв) и `submitCustomerReview()` в `data/customer.ts`. Новый компонент `components/customer/ReviewModal.tsx` — модалка с кликабельным выбором звёзд (1-5, свой стейт наведения, не переиспользовала `RatingStars` — тот компонент только для чтения) и комментарием; если сохранение не удалось, модалка не закрывается (даёт повторить, а не теряет введённое).

  **3. Красивый статус у покупателя.** `CustomerOrders.tsx` переведён на общие `lib/orderStatus.ts` (та же схема, что уже сделана для Admin/Farmer) вместо третьей копии STATUS_CLASSES/STATUS_KEYS. Добавила иконки на статус (`ORDER_STATUS_ICONS` в `lib/orderStatus.ts` — Clock/CheckCircle2/ChefHat/Truck/Award и т.д. по стадиям) и новый компонент `components/ui/StatusBadge.tsx` — то же визуальное решение, что и `StatusMenu`, но read-only (покупатель менять статус не может), иконку протянула и в сам `StatusMenu` (Admin/Farmer тоже получили иконки бесплатно, для единого стиля везде). Добавлена колонка "Отзыв" в таблицу заказов: кнопка "Оставить отзыв" для Completed-заказов без отзыва, "Отзыв оставлен" — если уже есть, "—" для остальных статусов.
- Файлы: Frontend/src/lib/orderStatus.ts, Frontend/src/components/ui/{StatusMenu,StatusBadge}.tsx, Frontend/src/components/customer/ReviewModal.tsx (новый), Frontend/src/data/{adminEntities,customer}.ts, Frontend/src/pages/{AdminOrders,FarmerOrders,CustomerOrders}.tsx, Frontend/src/locales/{ru,tj}/customer.json
- Решения/примечания: `tsc -b --noEmit`, `oxlint`, `vite build` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`, те же предсуществующие warnings). Не стала заводить отдельный Courier-кабинет для смены статуса — в проекте его ещё нет, статус доставки по-прежнему целиком ведёт Admin. Живой браузерный прогон не делала — стоит пройти путь: Admin переводит заказ в Completed → у покупателя приходит уведомление и появляется кнопка "Оставить отзыв" → после отправки кнопка меняется на "Отзыв оставлен".

## 01:10 — "Обзор" (AdminDashboard) переведён на реальные данные — дизайн не тронут

- Что сделано: Пользователь явно попросила: "пусть это в admin тоже будет настоящим, но дизайн не поменяй, мне так нравится" — про самую первую страницу админки ("Обзор"), которая с сессии 2026-07-22 сознательно оставалась мок-версией. Полностью заменила источник данных на реальный бэкенд, не тронув ни одного класса/пикселя разметки.

  **Откуда реальные данные:**
  - Обнаружился уже готовый эндпоинт `/analytics/admin/dashboard` (`AdminDashboardDto`) — считает totalUsers/Farmers/Customers/Couriers, totalOrders/ordersToday/ordersThisMonth, totalRevenue/revenueThisMonth (только Completed-заказы, раздел 10.4 ТЗ), revenueByMonth (реальные последние 6 месяцев) — уже был подключен на фронте как `useAdminAnalytics()` и использовался на отдельной странице "Аналитика" (`AdminStatistics.tsx`), которую я не трогала.
  - 4 карточки-статы: "Выручка" — `revenueThisMonth` + честный тренд/спарклайн из реального `revenueByMonth` (переиспользован уже существующий `computeMonthlyTrend()` из `lib/utils.ts`). "Заказы" — реальный `ordersThisMonth`, тренд посчитан на фронте группировкой уже загруженных `/orders` по месяцам (бэкенд готовой месячной разбивки по заказам не отдаёт). "Новые покупатели" — то же самое по `/users` (роль Customer, группировка по `createdAt`). "Активные фермеры" — количество реально подтверждённых фермеров (`useFarmers()`, уже только verified) с накопительным трендом по `joinedAt`. Раз данные реальные, сохранённый мок навсегда-положительный "рост" (зелёная стрелка вверх) стал честным: если метрика упала — красная стрелка вниз, если сравнивать не с чем (0 в прошлом месяце) — нейтральная "Новое".
  - "Продажи по регионам" (донат) — реальная сумма Completed-заказов по `Order.Region` (это свободный текст, который вводит покупатель при оформлении, не фиксированный список из 5 областей, как было в моке) — сколько регионов реально встретилось в заказах, столько и покажет; честный fallback-текст, если Completed-заказов пока нет.
  - "Посещения сайта" — самая нечестная часть мока: трекинга трафика в проекте нет вообще (проверила — ни одной Analytics/Visit/PageView-сущности в бэкенде). Не стала выдумывать — честно заменила на "Заказы по месяцам" с той же формой графика (текущий год vs прошлый год по месяцам), посчитано на фронте из тех же `/orders`. Года в легенде теперь настоящие (`new Date().getFullYear()`), а не захардкоженные 2025/2024.
  - "Последние заказы" — реальные последние 4 заказа из `/orders`, статус — из уже готовой на этой сессии реальной системы 11 статусов (`lib/orderStatus.ts`, те же цвета/иконки, что у Admin/Farmer/Customer), время — через уже существующий `timeAgo()` вместо голого времени "10:12".
  - "Топ фермеры" — реальные фермеры (`useFarmers()`, рейтинг уже настоящий из отзывов), выручка каждого — сумма его Completed-заказов, посчитанная на фронте (массовой выдачи выручки по всем фермерам бэкенд не даёт, только для самого фермера через его личный `/analytics/farmer/dashboard`).
  - "Популярные категории" — реальная доля активных объявлений по категориям (`useCategories()`, уже настоящий `productCount`), а не выдуманная доля выручки.
  - Честно НЕ стала резолвить настоящее ФИО покупателя в "Последних заказах" (лишний join через `/customer-profiles`+`/users` ради декоративной строки) — вместо этого "Клиент #id", как уже везде в `AdminOrders.tsx` на этой же панели — не понижение честности, а следование уже устоявшейся конвенции этой же страницы.
  - Важный технический момент: `useAdminStats`/`useOrdersByMonth` — единственные из новых функций `data/admin.ts`, что вызывают хуки (`useTranslation`) — пришлось поднять их вызов выше проверки loading/error в `AdminDashboard.tsx` (с безопасными пустыми заглушками), иначе нарушались Rules of Hooks (условный вызов хука после early return).
- Файлы: Frontend/src/data/admin.ts (переписан полностью), Frontend/src/pages/AdminDashboard.tsx (переписан полностью — карточки стали принимать данные через props вместо собственных хуков, чтобы не дёргать `/orders` пять раз), Frontend/src/locales/{ru,tj}/admin.json (секция `dashboard`: новые ключи `errorTitle/errorDescription/vsLastMonth/newMetric/ordersByMonth/noTopFarmersYet/noPopularCategoriesYet`, убраны обречённые `vsLastWeek/siteVisits/orderStatus.*/regions.*/months` — реального использования у них больше не осталось)
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`). Разметка/классы/цвета/структура карточек не менялись ни на пиксель — только источник чисел и honest-тексты там, где реальных данных не хватало. Живых данных сейчас всё ещё немного (2 фермера, почти нет Completed-заказов) — большинство карточек честно покажут маленькие/нулевые числа, это ожидаемо и это правильно, не баг.

## 02:10 — Клиентская панель: вернула доступ в каталог, полные тексты отзывов у Admin/Farmer, уведомления как в Instagram Direct, редактируемый профиль

- Что сделано: пользователь показала скриншот "Обзора" покупателя и указала на реальный баг — оформив заказ, покупатель не может вернуться в каталог, чтобы заказать снова (в сайдбаре `CustomerLayout` не было ссылки на каталог вообще). Заодно попросила: полный текст отзыва должен читаться у Admin/Farmer целиком (сейчас обрезан), и чтобы уведомления/бейджи у фермера и админа исчезали сами, как только заказ обработан — "как в инстаграм директ", а не висели до отдельного ручного клика.

  **1. Каталог доступен из личного кабинета покупателя.** `CustomerLayout.tsx` — в `NAV_ITEMS` добавлена ссылка "Каталог" (ведёт на публичный `/catalog`, вне `/customer/*`) — это и был баг, из-за которого покупатель "не может больше заказать": уйдя в свою панель, назад в каталог вернуться было некуда.

  **2. Отзывы читаются полностью.** `AdminReviews.tsx`/`FarmerReviews.tsx` — ячейка комментария была `max-w-80 truncate` (однострочная обрезка с многоточием), заменено на `max-w-80 whitespace-normal wrap-break-word` — текст переносится по строкам целиком, ничего не обрезается.

  **3. Уведомления — "открыл = прочитал", как в Instagram Direct.** Раньше `FarmerNotifications.tsx` требовал отдельного клика "Прочитано" по каждому уведомлению, а у Admin и Customer страницы уведомлений не было вообще (колокольчик в хедере ничего не открывал, хотя Customer уже реально получает уведомления о доставке/отзыве — писать ему было некуда их читать). Сделано:
  - Новый общий компонент `components/notifications/NotificationCenter.tsx` — при первом открытии страницы автоматически помечает все непрочитанные уведомления прочитанными (без ручных кнопок).
  - `data/farmer.ts` — в `useFarmerNotifications` добавлен тот же лёгкий pub/sub-мост, что уже был сделан для заказов фермера (`notifyFarmerOrdersChanged`) — теперь `notifyNotificationsChanged()` мгновенно обновляет бейдж-колокольчик в сайдбаре/хедере сразу после авто-прочтения, без перезагрузки страницы.
  - Добавлены `AdminNotifications.tsx` и `CustomerNotifications.tsx` (тонкие обёртки над `NotificationCenter`), новые роуты `/admin/notifications` и `/customer/notifications`, пункт "Уведомления" в сайдбарах Admin/Customer, колокольчик в хедере Admin теперь кликабельный (раньше `onClick` отсутствовал), у Customer колокольчик и бейдж заведены с нуля.
  - Бейдж пункта "Уведомления" в сайдбаре (Admin/Farmer/Customer) теперь окрашен в клэй (тревожный) цвет, отдельно от нейтрального серого бейджа "Заказы" — визуально различаются "нужно отреагировать" и "просто счётчик".
  - `FarmerNotifications.tsx` тоже переведён на `NotificationCenter` (кнопка "Прочитано" убрана за ненадобностью — авто-прочтение работает и там).

  **4. Профиль покупателя — самообслуживание вместо заглушки.** `CustomerProfile.tsx` был чисто read-only с текстом "редактирование появится позже" — при том что `PUT /api/customer-profiles/{id}` на бэкенде уже реально существует. Добавлена `updateCustomerProfile()` в `data/customer.ts` и модалка редактирования (регион/район/адрес по умолчанию) — тот же паттерн (react-hook-form + Modal), что уже работает у `FarmerProfile.tsx`. Тип покупателя (розница/опт) сознательно оставлен нередактируемым — это не самообслуживание, а отдельная бизнес-логика.

  Отдельно проговорила с собой: полный визуальный редизайн "под Золотое Яблоко/Alif Shop" — субъективная штука, которую не вижу вживую; вместо гадания сделала конкретные, однозначно устраняемые проблемы неудобства (нет входа в каталог, нет редактирования, неудобные уведомления) поверх уже готового единого стиля карточек (`StatCard`/`Card`), который в панели покупателя уже используется. Если нужна более глубокая визуальная переработка — стоит уточнить, что именно не устраивает по виду, отдельно от этих функциональных дыр.
- Файлы: Frontend/src/components/layout/{CustomerLayout,AdminLayout,FarmerLayout}.tsx, Frontend/src/components/notifications/NotificationCenter.tsx (новый), Frontend/src/pages/{FarmerNotifications,AdminNotifications,CustomerNotifications,AdminReviews,FarmerReviews,CustomerProfile}.tsx, Frontend/src/data/{farmer,customer}.ts, Frontend/src/App.tsx, Frontend/src/locales/{ru,tj}/{admin,farmer,customer}.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`, остальные warnings те же, что были). Живой браузерный прогон не делала — стоит проверить: (а) из панели покупателя реально попадаешь в каталог и можно оформить второй заказ; (б) длинный отзыв виден целиком у Admin/Farmer; (в) открыв "Уведомления" в любой из трёх панелей, бейдж в сайдбаре/колокольчике гаснет сам, без отдельного клика; (г) в профиле покупателя можно поменять регион/район/адрес и это сохраняется.

## 02:35 — У покупателя теперь видны дата+время заказа и дата+время получения

- Что сделано: пользователь попросила, чтобы при заказе показывались не только дата, но и время — причём отдельно для момента оформления заказа и отдельно для момента получения. Раньше `formatDate()` показывал только дату ("26 июля 2026"), а даты/времени получения не было вообще нигде на стороне покупателя.
  - Новая утилита `formatDateTime()` в `lib/utils.ts` — та же честная логика `formatDate()` (месяцы через i18n), плюс часы:минуты.
  - `CustomerOrders.tsx`: колонка "Дата" переименована в "Дата и время заказа" (теперь `formatDateTime(order.createdAt)`), добавлена новая колонка "Дата и время получения" — источник честный: `Delivery.DeliveredAt` (через уже существующий `useDeliveriesByOrder()` из `data/farmer.ts`, тот же generic-хук, что и у фермера, просто ещё один потребитель), а не `Order.CompletedAt` — это разные вещи (получение — это факт доставки курьером, завершение — административное закрытие заказа). Если доставки ещё не было — честно "Ещё не получен", а не пустая ячейка.
  - `CustomerDashboard.tsx`: список "Последние заказы" тоже показывает дату+время вместо просто даты (для второго таймстампа там нет места в компактном превью — полная детализация с "получено" осталась только на странице "Мои заказы").
- Файлы: Frontend/src/lib/utils.ts, Frontend/src/pages/{CustomerOrders,CustomerDashboard}.tsx, Frontend/src/locales/{ru,tj}/customer.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто. Живой прогон не делала — стоит проверить, что колонка "получен" корректно пустая для ещё не доставленных заказов и заполняется после того, как admin/курьер отметят доставку.

## 02:45 — То же самое (дата+время заказа и получения) — теперь и у фермера, и у админа

- Что сделано: сразу после предыдущей правки пользователь уточнила, что дата+время нужны не только покупателю, но и в панелях фермера и администратора. Применила ту же честную логику (`formatDateTime()`, `Delivery.DeliveredAt` через уже существующий `useDeliveriesByOrder()`) в обеих оставшихся таблицах заказов:
  - `FarmerOrders.tsx` — колонка "Дата" → "Дата и время заказа" (`formatDateTime`), добавлена новая колонка "Дата и время получения" (у фермера `deliveriesByOrderId` уже и так был загружен для колонки "Доставка" — просто переиспользовала).
  - `AdminOrders.tsx` — та же замена для createdAt, и добавлен полностью новый источник данных: раньше эта страница вообще не грузила `/deliveries` — добавлен `useDeliveriesByOrder()` (тот же общий хук, что и у Farmer/Customer) в комбинированный loading-guard и в новую колонку "Дата и время получения".
- Файлы: Frontend/src/pages/{FarmerOrders,AdminOrders}.tsx, Frontend/src/locales/{ru,tj}/{farmer,admin}.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`). Теперь все три роли (Customer/Farmer/Admin) видят одинаковую честную пару таймстампов по одному и тому же реальному источнику данных (Order.CreatedAt + Delivery.DeliveredAt), без расхождений в логике между панелями.

## 03:05 — Баг: "получено" всегда пусто даже у Завершённых заказов — нашла причину и честно исправила

- Что сделано: пользователь показала скриншот — у покупателя два заказа со статусом "Завершён", но колонка "Дата и время получения" у обоих пустая ("Ещё не получен"). Разобралась в причине: колонка была завязана строго на `Delivery.DeliveredAt` — реальную запись о доставке, которую в проекте физически некому создавать (нет курьерского кабинета, нет админ-формы назначения курьера/отметки о вручении). Admin просто вручную переводит заказ в статус "Завершён" через `StatusMenu` — это меняет `Order.Status`, а отдельная запись `Delivery` при этом не создаётся и не обновляется. В итоге колонка была обречена всегда показывать "не получен", даже для реально закрытых заказов — сама себе противоречила.

  Честная починка — не выдумывать `Delivery`, а использовать то, что реально есть: `Order.CompletedAt` бэкенд и так сам проставляет в момент первого перехода в статус Completed (см. `OrderService.UpdateAsync`) — это ближайший настоящий сигнал "когда заказ закрыли как полученный", пока полноценного курьерского флоу с отдельным подтверждением вручения нет. Добавила `resolveReceivedAt(status, completedAt, deliveredAt)` в `lib/orderStatus.ts`: если есть `Delivery.deliveredAt` — берёт его (более точный сигнал на будущее, когда курьерский флоу появится), иначе, если статус — Completed, берёт `Order.CompletedAt`, иначе честно "Ещё не получен". Применено во всех трёх таблицах заказов (Customer/Farmer/Admin).
- Файлы: Frontend/src/lib/orderStatus.ts, Frontend/src/pages/{CustomerOrders,FarmerOrders,AdminOrders}.tsx
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто. Это временный, но честный фолбэк, а не подделка данных — реальный `Delivery.DeliveredAt` по-прежнему в приоритете и будет использован автоматически, как только в проекте появится настоящее назначение курьера/отметка о доставке.

## 03:25 — Автоподсказка региона/района, код +992 не нужно набирать вручную

- Что сделано: пользователь попросила (1) автоподсказку регионов/районов при регистрации и оформлении заказа, чтобы не печатать вручную, (2) чтобы код "+992" в поле телефона был готов заранее и клиенту не нужно было его набирать. Не стала ждать её скриншота по третьему пункту (страница регистрации без скролла) — это отдельная визуальная правка, для которой она обещала прислать фото с уточнением.
  - Новый справочник `data/tajikistanGeo.ts` — реальные области Таджикистана (г. Душанбе, Хатлонская, Согдийская, ГБАО, РРП) и по каждой — реальный список районов/городов (не выдуманный, но и не официально исчерпывающий классификатор — рабочий набор для подсказки). Район теперь фильтруется по выбранному региону (`getDistrictsForRegion`), а если введённый регион не распознан — честно показывает районы всех областей, а не пустоту. Отдельного справочника сёл не заводила — это слишком не проверяемо на моём уровне знаний, поле "село" (только у фермера) осталось свободным текстом.
  - Новый `components/ui/Autocomplete.tsx` — комбобокс: печатаешь — список фильтруется на лету, кликаешь — подставляется, но всегда можно допечатать своё значение (не строгий select).
  - Новый `components/ui/PhoneInput.tsx` — код "+992" зафиксирован нередактируемым префиксом слева, поле принимает только оставшиеся 9 цифр, автоматически форматируя их через пробелы; итоговое значение вида "+992 XX XXX XXXX" уходит в форму как обычно.
  - Оба подключены через `Controller` (react-hook-form) в `Register.tsx` и `Checkout.tsx` — заменили обычные `Input` для полей телефон/регион/район в обеих формах.
- Файлы: Frontend/src/data/tajikistanGeo.ts (новый), Frontend/src/components/ui/{Autocomplete,PhoneInput}.tsx (новые), Frontend/src/pages/{Register,Checkout}.tsx
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`). Ждём от пользователя скриншот по пункту про скролл на странице регистрации — отдельная правка после уточнения.

## 03:50 — Страница регистрации без скролла вообще (по присланному скриншоту)

- Что сделано: пользователь прислала скриншот — на `/register` страница реально длиннее экрана, браузер её проскроллил (заголовок и кнопка "На главную" уже не видны сверху), и явно повторила: скролла быть не должно вообще, ни вниз, ни вверх. Разобрала, откуда высота: карточки выбора роли ("Я покупатель"/"Я фермер") были крупными вертикальными карточками с иконкой+описанием, плюс крупные отступы между всеми блоками — в сумме форма стабильно выше типичной высоты окна браузера.

  Пересобрала компактно:
  - Внешний контейнер `min-h-screen` → `h-screen overflow-hidden` — страница физически не может стать выше экрана (жёсткая гарантия отсутствия скролла, а не просто "постаралась чтобы влезло").
  - Селектор роли — вместо двух больших карточек (иконка+заголовок+описание, ~100px высотой) стал компактным сегмент-контролом в одну строку (~40px): просто две кнопки рядом на сером фоне, без описания.
  - Все отступы между блоками уменьшены (py-14→py-4, mb-8→mb-2, mt-7→mt-3, gap-5→gap-3 и т.д.), заголовок и подзаголовок уменьшены.
  - Пароль и подтверждение пароля — были отдельными строками, теперь в одну строку по два поля (экономия целой строки).
  - Кнопка регистрации — с `size="lg"` на `size="md"`.
  - Убрала из вёрстки описания ролей ("Хочу заказывать свежие продукты..."/"Хочу продавать свой урожай...") — они грузили высоту и с компактным сегмент-контролом уже не помещались; заодно убрала соответствующие теперь неиспользуемые ключи `roleCustomerDescription`/`roleFarmerDescription` из локалей.
- Файлы: Frontend/src/pages/Register.tsx, Frontend/src/locales/{ru,tj}/pages.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто. Живого браузера у меня нет, поэтому пиксель-в-пиксель на её конкретном экране не проверяла — компактность рассчитана с запасом на типичную высоту окна ноутбука, но если на её экране всё ещё не влезает без скролла (например, очень маленькое окно) — надо прислать новый скриншот, подожму ещё сильнее.

## 04:15 — Публичный профиль фермера (страница отзывов) + починена грамматика "N товаров"

- Что сделано: пользователь показала скриншот блока "Лучшие фермерские хозяйства" на главной и спросила, почему выглядит некрасиво и почему не получается прочитать отзыв. Разобралась в обеих причинах.

  **1. "1 товаров" — реальная грамматическая ошибка.** `product.json`'s `productsCount`/`reviewsCount` были одной плоской строкой без учёта склонения ("1 товаров", "2 товаров" — по-русски должно быть "1 товар", "2 товара", "5 товаров"). Перевела оба ключа на настоящие i18next-формы множественного числа (`_one`/`_few`/`_many`/`_other`) для ru — i18next сам выбирает нужную форму через `Intl.PluralRules` по числу. Для tj не трогала — там грамматического числа как в русском нет, один плоский вариант работает верно как есть.

  **2. "Не получается прочитать отзыв" — потому что читать было негде.** Карточка фермера (`FarmerCard.tsx`, используется в `BestFarmers.tsx` на главной) вела на `/catalog?farmer=id` — это просто фильтр каталога по товарам этого фермера, там нет ни био, ни отзывов вообще. Публичной страницы профиля фермера с отзывами в проекте не было ни одной — только личный (закрытый) `/farmer/profile` для самого фермера (редактирование) и `FarmerProfileCard` на странице товара (тоже без отзывов, только ссылка "Все товары" на тот же фильтр каталога).

  Добавила новую публичную страницу `pages/FarmerPublicProfile.tsx` на роуте `/farmers/:id` (специально во множественном числе — единственное `/farmer` уже занято закрытой панелью самого фермера, календарные пути не должны были пересечься): аватар, название хозяйства, регион/район/село, рейтинг, теги категорий, три честные карточки-статы (товаров/лет на платформе/с нами с), блок "О хозяйстве" (реальное описание фермера или честное "пока не добавлено"), и **полный список отзывов** через уже существующие `ReviewsSection` + `getCatalogReviewsByFarmerId()` (эти два готовых куска уже были в проекте для товара, просто не собраны в отдельную страницу фермера). Кнопка "Все товары" по-прежнему ведёт в каталог с фильтром — это отдельное, корректное действие, а не замена профилю.

  `FarmerCard.tsx` (карточки на главной/в каталоге) и заголовок в `FarmerProfileCard.tsx` (карточка на странице товара) теперь ведут на `/farmers/:id` вместо фильтра каталога.
- Файлы: Frontend/src/pages/FarmerPublicProfile.tsx (новый), Frontend/src/App.tsx, Frontend/src/components/product/{FarmerCard,FarmerProfileCard}.tsx, Frontend/src/locales/{ru,tj}/{product,pages}.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`). Живой браузерный прогон не делала — стоит открыть главную, кликнуть по карточке "Shahnoza" (0 отзывов) и по карточке "Хосилот" (4 отзыва) и убедиться, что обе страницы открываются и показывают честные данные.

## 04:35 — Реальный баг: оптовая цена от N кг обещалась, но никогда не применялась к сумме

- Что сделано: пользователь заметила, что на странице товара написано "от 10 кг — оптовая цена", но реальная сумма при заказе всё равно считается по розничной цене. Проверила код — так и есть: `wholesalePricePerKg`/`wholesaleMinimumQuantity` — настоящие поля из бэкенда (фермер их указывает при создании объявления, `FarmerProducts.tsx`), но в четырёх местах, где считается сумма к оплате, использовался только `retailPricePerKg`, без единой проверки "не набралось ли уже нужное количество для опта":
  - `context/CartContext.tsx` — общая сумма корзины.
  - `components/layout/MiniCart.tsx` — сумма по каждой строке в мини-корзине.
  - `pages/ProductDetails.tsx` — цена на кнопке "Добавить в корзину".
  - `pages/Checkout.tsx` — **самое важное**: `unitPrice`, который реально уходит в `OrderItem` на бэкенд при оформлении заказа, и сумма в панели чекаута.

  Раньше на странице товара блок про опт был просто текстом-обещанием, который никак не был связан с реальным расчётом — добавила единую функцию `getUnitPrice(product, quantity)` в `lib/utils.ts` (если `quantity` достигла `wholesaleMinimumQuantity` — берёт `wholesalePricePerKg`, иначе — `retailPricePerKg`) и подставила её во все четыре места разом, чтобы больше не расходились между собой. Плюс на странице товара — если сейчас выбранное количество уже даёт право на опт, вместо "от 10 кг" честно показывается "Оптовая цена применена" с галочкой, чтобы было видно, что оно реально сработало, а не просто написано.
- Файлы: Frontend/src/lib/utils.ts, Frontend/src/context/CartContext.tsx, Frontend/src/components/layout/MiniCart.tsx, Frontend/src/pages/{ProductDetails,Checkout}.tsx, Frontend/src/locales/{ru,tj}/pages.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`). `ProductCard.tsx` (карточка в сетке каталога) сознательно не трогала — там нет выбора количества, показывать стартовую розничную цену на карточке — обычная практика, опт всегда завязан на количество и уместен только там, где количество реально выбирается (страница товара, корзина, чекаут). Живой прогон не делала — стоит завести товар с оптовым порогом (например, `FarmerProducts.tsx`, "от 10 кг → дешевле") и проверить: добавить 10+ кг в корзину и убедиться, что сумма в мини-корзине, корзине и итоговом заказе считается уже по оптовой цене, а не по розничной.

## 05:20 — Остаток товара у фермера теперь честно "изначально X / осталось Y"

- Что сделано: тот же баг про оптовую цену навёл на связанный вопрос пользователя — при заказе остаток у фермера должен реально уменьшаться. Основная работа (списание/возврат остатка) сделана на бэкенде (см. `backend/progress/2026-07-26.md`) — на фронте нужно было честно показать разницу между "сколько было" и "сколько осталось", не заводя новое поле в базе (бэкенд его не хранит — `AvailableQuantity` просто уменьшается на месте).

  Восстановила "изначально" на лету: `изначально = текущий остаток + сумма Quantity по всем OrderItem этого объявления, чей заказ не в статусе Rejected/Cancelled` — та же арифметика, что и на бэкенде при списании/возврате, просто пересчитанная на фронте без похода в базу за историей. Добавила `useOrderItems()` в `data/farmer.ts` и `useAdminOrderItems()` в `data/adminEntities.ts` (те же generic "грузим всё, фильтруем на фронте" хуки, что и везде в проекте). В `FarmerProducts.tsx` и `AdminProducts.tsx` колонка "Остаток" теперь показывает вторую строку мелким шрифтом "из N изначально" — но **только если что-то реально было продано** (sold > 0), чтобы не захламлять таблицу бесполезным "10 из 10" для товаров, которые ещё никто не заказывал.
- Файлы: Frontend/src/data/{farmer,adminEntities}.ts, Frontend/src/pages/{FarmerProducts,AdminProducts}.tsx, Frontend/src/locales/{ru,tj}/{farmer,admin}.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто. Живой прогон не делала — стоит заказать товар и проверить, что у фермера в "Мои товары" остаток уменьшился и появилась строка "из N изначально", а после отмены/отклонения этого заказа остаток и строка честно вернутся к исходному состоянию (строка "изначально" исчезнет, раз sold снова 0).

## 05:35 — Расширила список районов Душанбе в автоподсказке

- Что сделано: пользователь заметила, что при выборе региона "г. Душанбе" список районов короткий (всего 4). Честно: это не баг — у Душанбе официально ровно 4 городских района (Сино, Исмоили Сомони, Фирдавси, Шохмансур), в отличие от областей, где районов физически больше. Добавила ещё три реально существующих, часто встречающихся в адресах района/микрорайона города (Зарафшон, Себзор, Испечак) — не как "официальные районы", а как дополнительные узнаваемые подсказки. Как и раньше, поле остаётся свободным текстом — если нужного варианта всё равно нет, можно просто напечатать его вручную (сама пользователь это подтвердила как ожидаемое поведение).
- Файлы: Frontend/src/data/tajikistanGeo.ts
- Решения/примечания: `tsc -b --noEmit` — чисто. Правка точечная, `vite build`/`oxlint` не гоняла отдельно — риска не несёт (просто больше строк в статическом массиве).

## 05:45 — Поле "Регион" теперь подсказывает и по названию города, не только по 5 областям

- Что сделано: пользователь уточнила — в чекауте подсказки казались "пустыми", потому что поле "Регион" предлагало только 5 официальных областей (г. Душанбе, Хатлонская, Согдийская, ГБАО, РРП). Многие пишут в "Регион" сразу название своего города (Худжанд, Бохтар, Кулоб...) — а такие названия у меня были только в списках районов, не в списке регионов, поэтому при вводе города подсказка не находила вообще ничего и выглядела как "не работает".

  Добавила `TAJIKISTAN_REGION_SUGGESTIONS` — список для поля "Регион" теперь включает не только 5 областей, но и все города/районы из всех областей разом (области + Хатлон + Согд + ГБАО + РРП + Душанбе, ~60 вариантов). "Район" по-прежнему честно показывает районы всех областей, если введённый регион не распознан как одна из 5 официальных (эта часть не менялась).
- Файлы: Frontend/src/data/tajikistanGeo.ts, Frontend/src/pages/{Register,Checkout}.tsx
- Решения/примечания: `tsc -b --noEmit`, `vite build` — чисто.

## 05:55 — Корзина и избранное больше не пропадают при обновлении страницы

- Что сделано: пользователь заметила реальный баг — обновление страницы (F5) стирало и корзину, и избранное, хотя ничего не оформлялось. Причина: `CartContext.tsx`/`FavoritesContext.tsx` держали список только в памяти React (`useState([])`) — при перезагрузке SPA пересоздаётся с нуля, вся память теряется. Добавила сохранение в `localStorage` (тот же приём, что уже используется в `AuthContext.tsx` для токена): при инициализации читаем сохранённое, при каждом изменении списка — сразу перезаписываем. `clearCart()` после реального оформления заказа по-прежнему честно очищает корзину — это единственный случай, когда она должна пропадать, как пользователь и уточнила.
- Файлы: Frontend/src/context/{CartContext,FavoritesContext}.tsx
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (те же предсуществующие warnings про only-export-components в этих же файлах, что были и раньше). Живой прогон не делала — стоит добавить товар в корзину/избранное, обновить страницу (F5) и убедиться, что всё осталось на месте, а после реального оформления заказа корзина по-прежнему пустеет.

## 06:10 — Выпадающее меню статуса больше не ломается в таблице + админ не лезет в зону фермера

- Что сделано: пользователь прислала скриншот — открытое меню статуса в "Заказы" у фермера визуально ломалось/обрезалось, и попросила проверить то же самое у админа (там "вообще ужасно"). Плюс явно попросила разделить зоны ответственности: то, что решает фермер, не должно быть доступно админу, пока заказ не дойдёт до "Готов к выдаче" — дальше только админ.

  **1. Причина визуального бага.** Таблицы заказов обёрнуты в `<div className="overflow-x-auto">` (горизонтальный скролл на узких экранах) — а `StatusMenu` рисовал выпадающее меню обычным `position: absolute` внутри строки таблицы. По спецификации CSS, если `overflow-x` не `visible`, второй `overflow-y` тоже перестаёт быть `visible` — меню обрезалось по вертикали этим же контейнером. Баг был в обеих панелях (Admin и Farmer), потому что обе используют один и тот же `StatusMenu`.

  Починила: меню теперь рисуется через портал в `document.body` (`createPortal`, тот же приём, что уже использует `Modal.tsx`) с `position: fixed` по реальным координатам кнопки (`getBoundingClientRect()`) — больше не зависит от overflow родительских контейнеров.

  **2. Разделение зон Admin/Farmer.** У `AdminOrders.tsx` раньше можно было выставить заказу вообще любой статус из всех 11 в любой момент — в том числе те, что по смыслу решает только фермер (Принят/Отклонён/Готовится). Добавила `getAdminNextStatuses()` в `lib/orderStatus.ts` — зеркало уже существующего `getFarmerNextStatuses()`: пока заказ не в статусе "Готов к выдаче" или позже, меню статуса у Admin заблокировано (иконка замка + подсказка "Пока заказ обрабатывает фермер..."), а с "Готов к выдаче" открывается обычная цепочка (Назначен курьер → Забран → В пути → Доставлен → Завершён, плюс "Отменён" доступен на любом из этих шагов).
- Файлы: Frontend/src/components/ui/StatusMenu.tsx, Frontend/src/lib/orderStatus.ts, Frontend/src/pages/AdminOrders.tsx, Frontend/src/locales/{ru,tj}/admin.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто. Живой прогон не делала — стоит открыть меню статуса в обеих панелях и проверить, что оно больше не обрезается, а у Admin для заказа со статусом раньше "Готов к выдаче" меню честно заблокировано с подсказкой.

## 06:25 — "Избранное" и "Корзина" теперь в сайдбаре личного кабинета покупателя

- Что сделано: пользователь заметила, что в сайдбаре панели покупателя нет ни "Избранного", ни "Корзины" — только Обзор/Каталог/Мои заказы/Уведомления/Профиль. Добавила оба пункта с бейджем-счётчиком (как уже было у "Уведомлений").

  Отдельных страниц под них заводить не стала — переиспользовала то, что уже реально работает в публичном хедере: "Избранное" ведёт на `/catalog?favorites=1` (тот же фильтр каталога, что уже собран в `Catalog.tsx` — своя честная пустая заглушка, заголовок "Избранное" и т.д.), "Корзина" — на `/checkout` (та же страница, где и так виден полный список товаров в корзине с количеством/удалением построчно, а не только форма доставки). Бейджи считают реальные `favoriteIds.length` (`FavoritesContext`) и `totalItems` (`CartContext`) — те же значения, что уже показывает счётчик в публичном хедере, цвета бейджей тоже подобрала под него (избранное — clay, корзина — grove).

  Мелкая техническая деталь: "Каталог" и "Избранное" оба указывают на `/catalog` (второй — с `?favorites=1`), поэтому обычная проверка активного пункта через `startsWith` не различила бы их — расписала отдельно для этих двух через `location.search`.
- Файлы: Frontend/src/components/layout/CustomerLayout.tsx, Frontend/src/locales/{ru,tj}/customer.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (те же предсуществующие warnings). Живой прогон не делала — стоит добавить товар в избранное/корзину и проверить, что в сайдбаре появляются правильные цифры и клик по пунктам ведёт куда надо.

## 06:35 — Переключатель тёмной/светлой темы теперь есть и в Admin/Farmer/Customer

- Что сделано: пользователь попросила добавить переключение тёмного/светлого фона в панели Admin, Farmer и Customer, покрасивее и с плавным переходом. Разобралась — вся инфраструктура темы уже была полностью готова и качественно сделана (`ThemeContext.tsx`: класс `dark` на `<html>`, сохранение в localStorage, отслеживание системной темы, и даже плавный кросс-фейд через View Transitions API при переключении — то самое "плавно", о чём просила), а все три панели уже сплошь усеяны классами `dark:...` по всей вёрстке (тёмная тема технически уже работала бы, если её включить). Не хватало только самой кнопки-переключателя (`ThemeToggle.tsx` — анимированная иконка солнце/луна) — она была только в хедере публичного сайта, в личных кабинетах её просто негде было нажать.

  Добавила `<ThemeToggle />` в хедер всех трёх панелей, рядом с переключателем языка — тот же самый компонент, что и на публичном сайте, ничего нового изобретать не пришлось.
- Файлы: Frontend/src/components/layout/{AdminLayout,FarmerLayout,CustomerLayout}.tsx
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (те же предсуществующие warnings). Живой прогон не делала — стоит зайти в любую из трёх панелей и нажать на иконку солнца/луны в хедере, проверить что переход плавный и все элементы читаемы в тёмном режиме (учитывая, что dark: классы уже были в вёрстке всю сессию, риск "ужасного" вида низкий, но глазами не проверяла).

## 14:21 — Таджикский перевод приведён к единой терминологии, исправлены реальные ошибки перевода

- Что сделано: пользователь написала "перевод на таджикский он какой-то странный, сделай нормальным" — без конкретики, что именно не так. Ранее уже было известно (из памяти сессий), что tj-локаль делалась мной же без участия носителя языка, поэтому это был давно известный риск. Вместо гадания запустила два параллельных фоновых агента, которые построчно сверили все 10 файлов `tj/*.json` (1721 строка) с их `ru/*.json` аналогами по конкретным критериям (кальки, неверный выбор слова, несогласованная терминология, грамматика, орфография ғ/қ/ҳ/ҷ/ӣ/ӯ, чрезмерная русификация/пуризм, пунктуация) — каждый вернул структурированный список находок (файл/путь ключа/текущий текст/проблема/предлагаемое исправление). Из двух независимых отчётов синтезировала общую картину и применила правки.

  **Главный вывод обоих агентов независимо: не орфография (она в порядке почти везде), а разнобой терминологии** — один и тот же смысл в разных файлах/местах переведён разными словами, из-за чего строки читаются так, будто их писали разные люди. Это и есть то самое "странно", на что жаловался пользователь. Исправлено сквозным образом по всем файлам:
  - "курьер" → "курер" везде (в таджикской кириллице нет мягкого знака — сама же местами уже писала "курер" правильно, местами "курьер" по ошибке).
  - "Мизоҷ" (клиент/темперамент, редкое слово) → "Харидор" (покупатель, уже стандарт в customer.json) — везде, где речь о покупателе в колонках/лейблах Admin и Farmer.
  - "Тақриз" / "Шарҳ" (два разных слова для "отзыв") → унифицировано на "Шарҳ" (уже доминирующий вариант в customer/farmer/pages).
  - "администратор" (русская калька) → "маъмур" (уже стандарт в profile.pendingHint/rejectedHint) в statusLockedHint фермера.
  - "систематии" (= "систематический", ложный друг) → "системавии" (= "системный", как и было задумано в settings.emptyDescription).
  - "майдончаи" (= "площадка/лужайка") → "платформаи" — в dashboard.greetingSubtitle, чтобы совпадать с остальными местами, где "платформа" — сохранённое заимствование.
  - "Тамом шудааст" / "Мавҷуд нест" (два варианта "нет в наличии") → унифицировано на "Мавҷуд нест".
  - "Дурнамо" (панорама/перспектива — слишком литературное слово для пункта меню "Обзор") → "Хулоса" (сводка) — во всех трёх панелях, оба агента независимо это отметили.
  - "Расмиятдарории фармоиш" / "расмият кардани фармоиш" / "фармоиш расмият дода шуд" (три разных формы "оформить заказ" в одном и том же checkout-разделе pages.json) → унифицировано на форму "расмият кардани/карда" (уже была в miniCart.checkout).

  **Реальные ошибки (не просто стиль):**
  - `common.json`/`farmer.json` — "Ҳаҷми ҳадди ақали файл — 5 МБ" буквально означало "**минимальный** размер файла — 5 МБ" вместо "**максимальный**" (ҳадди ақал = минимум, а не максимум) — реальная смысловая ошибка в предупреждении о загрузке фото, теперь "Андозаи ҳадди аксари файл — 5 МБ".
  - `farmer.json` products.form.titlePlaceholder — плейсхолдер был не переведён вообще, оставлен на русском ("Помидор грунтовый, свежий сбор") — теперь "Помидори заминӣ, ҳосили тару тоза".
  - `farmer.json` documents.uploadHint — "файловый API" (русское прилагательное с русским окончанием прямо внутри таджикского текста) → "API-и файлӣ".
  - `data.json` — "урюк" (сушёный целый абрикос с косточкой) использовалось вместо "зардолу" (абрикос — уже правильно используется в других местах того же файла), плюс тавтология "урюки хушк" ("сушёный сушёный-абрикос") — исправлено на "зардолуи хушк" во всех вхождениях (категория, товар 401).
  - `data.json` farmers.1.bio — нарушен порядок изафета: "кабудии кушоди замин" (грамматически сломано) → "кабудии замини кушод" (правильно, как и в specifications того же файла).
  - `data.json` farmers.1/7.story — "пойгоҳи кӯҳҳои/қаторкӯҳи" (= "у основания/постамента гор", неверное слово) → "домани кӯҳҳои/қаторкӯҳи" (= "у подножия", уже правильно используется в farmers.8/products.204 того же файла).
  - `data.json` farmers.8.story — "меваро...саралои карда сар кард" (порядок деепричастия подразумевал "начал, уже отсортировав" — задом наперёд) → "саралои кардани меваро...сар кард" (начал сортировать).
  - `data.json` reviews.replies[1] — показатель прямого дополнения "-ро" был приклеен не к тому слову ("борбандиро" вместо "хостаҳоро") — исправлено.
  - Мелкие точечные правки: `pages.json` login.loginErrorFallback (было "невозможен" вместо "не удался", plus несовпадающая с остальным приложением формулировка "попробуйте ещё раз") приведена к стандартному "Аз нав кӯшиш кунед."; contact.description (пропущенный союз "ки"); productDetails.share ("Мубодила" = обмен/бартер → "Паҳн кардан" = поделиться); productDetails.originRegion (более естественное "Минтақаи истеҳсол"); `sections.json` hero.farmerVerifiedTitle (полное предложение → форма-бейдж, как остальные "verified"-места), faq.title (тяжёлая калька → "Саволҳои маъмул"), cta.title ("санҷед"=проверить → "чашед"=попробовать на вкус, для еды правильнее), deliveryProcess.title (добавлено "хона", чтобы не путалось с предлогом "дар"); `layout.json` authPanel.quote (калька "фарқ...бо" вместо естественного "фарқ...нисбат ба").
- Файлы: Frontend/src/locales/tj/{admin,farmer,customer,common,data,pages,product,sections,layout}.json
- Решения/примечания: JSON-валидность всех 10 файлов проверена через `node -e "JSON.parse(...)"`. `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`, не моя). Оставила без изменений несколько пунктов низкой уверенности, которые агенты явно пометили как спорные/требующие живого носителя (например, farmers.status.verified/rejected — одно/два слова; "Дар интизорӣ" для pending; ui.json breadcrumbs/themeToLight — не тронуты, чтобы не внести новую странность вместо старой без уверенности). Родного носителя таджикского для финальной проверки у меня по-прежнему нет — если после этой правки пользователь (или носитель языка) укажет ещё конкретные места, стоит доразобрать точечно.

## 14:37 — Чат между покупателем и фермером по заказу (Conversation/ChatMessage) — реализован на фронте с нуля

- Что сделано: пользователь написала, что бэкенд ей говорил про какой-то чат, попросила посмотреть, есть ли он, и если да — красиво реализовать на фронте. Проверила бэкенд: `Conversation`/`ChatMessage` (раздел 8.15–8.16 ТЗ) полностью готовы — сущности, `ConversationController`/`ChatMessageController` (типовой generic CRUD `/api/conversations`, `/api/chat-messages`, без серверной фильтрации — та же схема, что и везде в проекте), сервисы, валидаторы, тесты. На фронте страницы под это не было вообще — реализовала с нуля по образцу уже готового спецификацией флоу (раздел 13.11 ТЗ: один чат на заказ, доступ только у Customer/Farmer этого заказа).

  **Важная нестыковка типов, с которой пришлось разобраться.** `Order.CustomerId`/`Order.FarmerId` — это `CustomerProfile.Id`/`FarmerProfile.Id` (раздел 9 ТЗ), а `Conversation.CustomerId`/`Conversation.FarmerId` — прямой FK на `User.Id` (раздел 8.15 ТЗ, специально указано в самой схеме). Это та же нестыковка, что уже решалась в сессии для уведомлений (`notifyCustomerAboutOrderStatus`, `submitCustomerOrder`) — резолвится через `/customer-profiles`/`/farmer-profiles`.

  **Новый файл `data/chat.ts`** — `ConversationDto`/`ChatMessageDto`, `useFarmerUserIdMap()`/`useCustomerUserIdMap()` (id профиля → User.Id, тот же приём "грузим всё, сопоставляем на фронте"), `useConversations()`/`useChatMessagesAll()`/`useChatMessages(conversationId)`, `getOrCreateConversation(orderId, customerUserId, farmerUserId)` (POST не возвращает id — как и везде, находим через повторный GET по `orderId`, уникальность `Order 1 — 0..1 Conversation` гарантирована на бэкенде), `sendChatMessage()`, `markChatMessageRead()`.

  **Новые компоненты `components/chat/`:**
  - `ChatModal.tsx` — модалка-мессенджер (тот же `Modal.tsx`-портал, что и у отзыва/редактирования профиля): шапка с аватаром и именем собеседника, лента сообщений с пузырями (свои — справа, зелёные; чужие — слева, серые, с автоскроллом вниз), поле ввода с отправкой по Enter (Shift+Enter — перенос строки). Реального времени/SignalR в проекте нет и ТЗ этого не требует (раздел 13.11) — пока модалка открыта, работает простой поллинг раз в 4 секунды. Чужие непрочитанные сообщения автоматически помечаются прочитанными при получении — тот же приём "открыл = прочитал", что и в `NotificationCenter`.
  - `ChatButton.tsx` — компактная круглая кнопка-иконка с бейджем непрочитанных (тот же визуальный язык, что и бейдж-колокольчик в хедерах панелей).

  **Подключение в таблицы заказов.** И `CustomerOrders.tsx`, и `FarmerOrders.tsx` получили новую колонку "Чат" с кнопкой на каждой строке заказа и живым счётчиком непрочитанных (грузятся все `Conversation`+`ChatMessage` разом на странице, как и остальные списки в проекте, и сопоставляются по `orderId`/`conversationId`). У покупателя имя собеседника — реальное название хозяйства (переиспользован уже существующий публичный каталог фермеров `useFarmers()` из `catalogStore.ts`, там уже есть и `farmName`, и `userId`); у фермера — честный "Харидор #id" (никакого публичного справочника имён покупателей в проекте нет, как и в остальных местах, где фермер видит покупателя).
- Файлы: Frontend/src/data/chat.ts (новый), Frontend/src/components/chat/{ChatModal,ChatButton}.tsx (новые), Frontend/src/pages/{CustomerOrders,FarmerOrders}.tsx, Frontend/src/locales/{ru,tj}/{customer,farmer}.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`, остальные warnings — предсуществующие или тот же паттерн `useAsync`, что и во всех остальных хуках проекта). Admin в чат сознательно не пускала — по ТЗ доступ только у Customer/Farmer конкретного заказа, у админа для этого предусмотрен отдельный `SupportTicket` (раздел 13.13 ТЗ), который пока не реализован и не запрашивался. Живой браузерный прогон не делала — стоит открыть заказ и с той, и с другой стороны (например, "Хосилот" и её реальный покупатель) и проверить, что чат создаётся один раз на заказ, сообщения доходят в обе стороны, а бейдж непрочитанных гаснет при открытии модалки.

## 14:50 — Кнопка "Чат" переставлена ближе к началу строки таблицы

- Что сделано: пользователь не смогла найти кнопку чата — она была последней (8-й/9-й) колонкой в уже и без того плотных таблицах заказов (`CustomerOrders.tsx`/`FarmerOrders.tsx`), и на экране с сайдбаром реально уезжала за край без горизонтального скролла. Переставила колонку "Чат" сразу вторым столбцом (сразу после номера заказа) в обеих таблицах — теперь видна без скролла на типичной ширине экрана.
- Файлы: Frontend/src/pages/{CustomerOrders,FarmerOrders}.tsx
- Решения/примечания: `tsc -b --noEmit` — чисто. Чисто визуальная перестановка колонок, логика не менялась.

## 15:22 — Чат теперь можно начать до заказа ("Спросить у фермера" на странице товара) + отправка фото

- Что сделано: пользователь уточнила, что чат должен работать не только по уже оформленному заказу, но и до него — чтобы покупатель мог спросить у фермера что-то про товар с его карточки, в том числе с фото. Это потребовало доработки бэкенда (`Conversation.OrderId` стал необязательным, `ChatMessage` получил `ImageUrl`, новый upload-эндпоинт — подробности в `backend/progress/2026-07-26.md`) и заметного расширения фронтовой части чата.

  **`data/chat.ts`:** `ConversationDto.orderId` теперь `number | null`. `getOrCreateConversation(orderId, customerUserId, farmerUserId)` принимает `orderId: number | null` — если `null`, ищет/создаёт чат по паре `customerUserId`+`farmerUserId` вместо `orderId` (та же find-or-create логика, просто другой ключ сопоставления). Новая `uploadChatImage(conversationId, senderId, caption, file)` — multipart-загрузка на `/chat-messages/upload`, тот же приём, что уже есть у `uploadProductImage`/`uploadAvatar`. `ChatMessageDto` получил `imageUrl: string | null`.

  **`ChatModal.tsx`:** `orderId`/`orderNumber` стали `| null` — если заказа нет, в шапке вместо "О заказе X" честно "Вопрос о товаре". Добавлена кнопка-скрепка рядом с полем ввода: открывает системный выбор файла, клиентская проверка формата (jpg/png/webp) и размера (до 5 МБ) перед отправкой — тот же лимит, что и на бэкенде, чтобы не тратить впустую аплоад на заведомо отклонённый файл. Загруженное фото рендерится прямо в пузыре сообщения (`<img>` сверху, подпись — под ним, если была введена), с отдельным состоянием загрузки (спиннер на кнопке-скрепке), не блокирующим текстовый ввод.

  **Новая точка входа — `ProductDetails.tsx`:** рядом с уже существующей ссылкой на фермера добавлена кнопка "Спросить у фермера" (иконка сообщения). При клике: если гость — тост с просьбой войти и редирект на `/login`; если вошёл не как покупатель (Farmer/Admin) — честный тост "написать фермеру может только покупатель"; иначе открывается `ChatModal` с `orderId={null}`, `farmerUserId` — уже готовое поле `farmer.userId` из публичного каталога (`useFarmers()`/`catalogStore.ts`), `customerUserId` — `user.userId` из `AuthContext` напрямую (Conversation.CustomerId — это и есть User.Id, лишний поход за профилем не нужен).
- Файлы: Frontend/src/data/chat.ts, Frontend/src/components/chat/ChatModal.tsx, Frontend/src/pages/{ProductDetails,CustomerOrders,FarmerOrders}.tsx, Frontend/src/locales/{ru,tj}/{pages,customer,farmer}.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`). Кнопку "Спросить у фермера" сознательно не показала фермеру/админу/гостю активной без явной причины — вопрос "фермер → фермер" или "админ → фермер" вне бизнес-логики проекта, не запрашивался. Живой браузерный прогон не делала — стоит открыть страницу любого товара под покупателем, нажать "Спросить у фермера", отправить сообщение и фото, затем проверить с фермерской стороны (там чат теперь тоже придёт, просто без номера заказа — как "Вопрос о товаре"), что оба видят переписку и фото открывается.

## 15:46 — Отдельная страница "Сообщения" вместо чата внутри таблицы заказов (у фермера и покупателя)

- Что сделано: пользователь протестировала чат живьём и указала на две проблемы: (1) непонятно, как фермеру увидеть и ответить на переписку, начатую покупателем — по факту у фермера был точно такой же чат внутри таблицы заказов, но что-то в поведении читалось как "не работает"; (2) явно попросила убрать чат из таблицы заказов целиком, и у покупателя, и у фермера. Разобралась в причине "не работает" — это оказался реальный бэкенд-баг (см. `backend/progress/2026-07-26.md`, запись 15:46: фото-сообщение без подписи никогда не помечалось прочитанным из-за слишком строгого валидатора, что выглядело как зависание чата). А вместо чата, разбросанного по строкам таблицы заказов, сделала для чата отдельное место — так он не путается с заказами и есть один явный способ его найти и ответить.

  **Убрано:** колонка "Чат" и кнопка на каждой строке из `CustomerOrders.tsx`/`FarmerOrders.tsx` (были добавлены в предыдущей правке этой же сессии) — таблицы вернулись к прежнему виду. `ChatButton.tsx` удалён как более не используемый.

  **Добавлено — новая страница "Сообщения" в обеих панелях** (`/customer/messages`, `/farmer/messages`, пункт в сайдбаре с бейджем непрочитанных, между "Заказы" и "Отзывы"/"Уведомления"): список всех переписок пользователя (и по заказам, и общих "вопрос о товаре"), отсортированный по последнему сообщению, с аватаром собеседника, превью последнего сообщения (или "Фото", если сообщение без подписи), меткой "О заказе X" / "Вопрос о товаре" и бейджем непрочитанных — клик открывает уже существующую `ChatModal`. Общая логика вынесена в переиспользуемый `components/chat/ConversationsList.tsx` (принимает `ns`, `currentUserId` и две функции разрешения отображаемого имени/номера заказа — конкретика по ролям осталась в тонких страницах-обёртках `FarmerMessages.tsx`/`CustomerMessages.tsx`, тот же паттерн, что уже используется у `NotificationCenter`).

  Бейдж непрочитанных в сайдбаре — та же логика, что уже была у чата внутри таблиц заказов (просто переехала в layout): грузятся все `Conversation`+`ChatMessage` пользователя, считаются непрочитанные чужие сообщения по своим `Conversation.id`.
- Файлы: Frontend/src/components/chat/{ConversationsList,ChatButton(удалён)}.tsx, Frontend/src/pages/{FarmerMessages,CustomerMessages,CustomerOrders,FarmerOrders}.tsx, Frontend/src/components/layout/{FarmerLayout,CustomerLayout}.tsx, Frontend/src/App.tsx, Frontend/src/locales/{ru,tj}/{customer,farmer}.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`). **Важно:** бэкенд-фикс валидатора (фото без подписи) требует перезапуска `MarketTJ.WebApi`, чтобы подействовать на живом сервере — сейчас он ещё крутится со старой сборкой (пользователь перезапускала его для тестирования уже после моей более ранней сборки с миграцией, но до фикса валидатора). Живой прогон не делала — стоит перезапустить бэкенд, затем зайти в "Сообщения" с обеих сторон и проверить, что и текстовые, и фото-сообщения нормально помечаются прочитанными (бейдж гаснет), без "зависаний".
