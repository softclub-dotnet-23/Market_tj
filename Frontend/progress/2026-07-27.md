## 06:34 — Бейдж "Заказы" у админа: непрочитанные, а не общий счётчик

- Что сделано: пользователь заметила баг — в сайдбаре у пункта "Заказы" стояла цифра 6, хотя все 6 заказов на странице уже "Завершён" и явно просмотрены. Разобралась: бейдж считал `orders?.length ?? 0` — буквально общее число заказов в базе, независимо от статуса и того, смотрел ли админ список вообще. Число никогда не уменьшалось.

  Ожидаемое поведение (сформулировано пользователем): зашла в раздел "Заказы", увидела все 10 — бейдж должен обнулиться после просмотра; если потом пришёл 1 новый заказ, бейдж должен показать "1", а не "11".

  Реализовала как "непрочитанное с последнего просмотра", тем же принципом, что уже используется для уведомлений, только источник правды — не серверный `IsRead`, а локальный: `Order` у админа не привязан к конкретному ответственному пользователю (в отличие от `Notification.UserId`), поэтому "просмотрено" — честно фронтовое понятие. `data/adminEntities.ts`: `markAdminOrdersSeen(orders)` запоминает в `localStorage` максимальный увиденный `Order.Id`; `useAdminOrdersUnseenCount(orders)` считает заказы с `id` больше сохранённого. Оповещение между `AdminOrders.tsx` (страница, вызывает `markAdminOrdersSeen` в `useEffect` после каждой загрузки списка) и `AdminLayout.tsx` (бейдж в сайдбаре) — тот же pub/sub-приём, что уже был у `notifyFarmerOrdersChanged`/`notifyNotificationsChanged`.
- Файлы: Frontend/src/data/adminEntities.ts, Frontend/src/components/layout/AdminLayout.tsx, Frontend/src/pages/AdminOrders.tsx
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`). Цвет бейджа не меняла — у фермера аналогичный счётчик "Заказы" (по `Pending`) тоже нейтральный серый, а не тревожный clay, для единообразия оставила так же. Живой прогон не делала — стоит зайти в админку, посмотреть на цифру у "Заказы", зайти в сам раздел и проверить, что бейдж обнулился, затем (например, через тестовый заказ с другой роли) добавить один новый заказ и убедиться, что бейдж показывает именно "1".

## 06:48 — Таблицы заказов (Admin/Farmer) показывают, что именно заказано

- Что сделано: пользователь посмотрела на панель фермера — заказ есть, но что именно заказал покупатель, непонятно (только номер заказа, клиент, сумма). Указала, что то же самое нужно и админу. Разобралась: `OrderItem` (реальные позиции заказа — `productName`+`quantity`) на бэкенде давно есть, и хуки для их загрузки (`useOrderItems()`/`useAdminOrderItems()`, "грузим всё, фильтруем на фронте") уже были в проекте с прошлой сессии — просто ни на одной из двух таблиц заказов их не выводили.

  Добавила новую колонку "Товары" — в `FarmerOrders.tsx` (после "Клиент") и `AdminOrders.tsx` (после "Фермер"): список позиций заказа построчно, `Название товара × количество`, честное "—" если почему-то нет ни одной позиции. Единицу измерения (кг/шт/пучок) не приписывала — `OrderItem` её не хранит (ни на бэкенде, ни в существующих DTO), а достраивать через дополнительный join `OrderItem → ProductListing → Product` ради подписи "кг" — риск показать неверную единицу, если у товара она не килограммовая; просто число рядом с названием честнее, чем угаданная единица.
- Файлы: Frontend/src/pages/{FarmerOrders,AdminOrders}.tsx, Frontend/src/locales/{ru,tj}/{admin,farmer}.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`). Не гейтила страницу на загрузку `orderItems` (нет отдельного `PageLoader` до их подгрузки) — тот же нестрогий подход, что уже был у "originally X" в `FarmerProducts.tsx`/`AdminProducts.tsx`: таблица рисуется по заказам сразу, колонка "Товары" на мгновение показывает "—", пока позиции не подтянутся. Живой прогон не делала — стоит открыть "Заказы" в обеих панелях и убедиться, что для каждого заказа видно, что реально было куплено.

## 06:51 — Колонка "Товары" резала текст до нечитаемого + настоящее имя покупателя вместо "Клиент #1"

- Что сделано: пользователь прислала скриншот только что добавленной колонки "Товары" — текст резался посреди слова и даже посреди числа ("Картофель молодой × 1...", "Грецкий орех очищенн..."), и отдельно спросила, почему везде "Клиент #1" вместо настоящего имени, явно потребовав, чтобы и фермер, и админ видели, кто заказал.

  **1. Обрезание текста.** Причина — сама и заложила: `max-w-56 truncate` на контейнере плюс `truncate` на каждой строке принудительно резали текст до одной строки с многоточием, не давая таблице расшириться и включить уже существующий горизонтальный скролл (`overflow-x-auto` на обёртке). Убрала `truncate`, заменила `max-w-56` на `min-w-56` (колонка не сжимается меньше читаемой ширины, но свободно растёт, а таблица честно скроллится вбок при нехватке места) — многострочные названия товаров теперь переносятся по словам, а не обрезаются.

  **2. Имя покупателя.** Реальная причина оказалась глубже, чем просто "не резолвили" — фермеру взять `User.FullName` было физически неоткуда (`/api/users` — только для Admin). Разобралась и починила на бэкенде (см. `backend/progress/2026-07-27.md`): `GetOrderDto` теперь сразу приходит с готовыми `customerFullName`/`customerPhone`. На фронте — `AdminOrderDto`/`FarmerOrderDto` получили эти поля; колонка "Клиент" в `AdminOrders.tsx`/`FarmerOrders.tsx` показывает `order.customerFullName ?? t("orders.customerLabel", {id})` — честный откат на "Клиент #id", если имя почему-то не резолвилось (например, легаси-данные).
- Файлы: Frontend/src/pages/{FarmerOrders,AdminOrders}.tsx, Frontend/src/data/{farmer,adminEntities}.ts
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`). Заодно нашла и сообщила пользователю отдельный, более глубокий баг: поле "Имя получателя" на `Checkout.tsx` собирается, но никуда не отправляется (та же судьба у телефона — используется только для экрана "Заказ принят" самого покупателя, хотя текст там обещает, что фермер позвонит по этому номеру). Не чинила сама — это отдельное решение (нужен ли "получатель", отличный от аккаунта покупателя, или хватает имени из профиля), жду, что скажет пользователь. Живой прогон не делала — стоит открыть "Заказы" в обеих панелях после перезапуска бэкенда и проверить, что видно настоящее имя, а не "Клиент #id", и что длинные названия товаров переносятся, а не обрезаются.

## 07:05 — Колонка "Товары": вместо переноса по строкам — компактно, "ещё N"

- Что сделано: перенос по строкам (правка 06:51) решил проблему обрезанного текста, но создал новую — пользователь прислала скриншот, где заказы с 2-3 товарами стали занимать заметно больше высоты, из-за чего появился неприятный вертикальный скролл всей таблицы. Предложила вариант "показывать простой порядковый номер 1, 2, 3 вместо реального номера заказа" — объяснила, почему это не решение (реальный `MTJ-...` номер нужен, чтобы однозначно ссылаться на конкретный заказ, например при разборе с поддержкой; порядковый номер "плывёт" при сортировке/пагинации) — и предложила вместо этого решить саму причину: высоту строки.

  Новый общий компонент `components/ui/OrderItemsCell.tsx`: показывает только первую позицию заказа в одну строку (с обычным `truncate` — так же, как уже сделано у колонки "Адрес"), а если позиций больше одной — остальные скрыты за ссылкой "ещё N", которая по клику разворачивает/сворачивает список. Строка таблицы снова компактная (одна строка по умолчанию), но при этом ничего не обрезается посреди слова — просто скрыто целыми позициями, а не посимвольно.
- Файлы: Frontend/src/components/ui/OrderItemsCell.tsx (новый), Frontend/src/pages/{FarmerOrders,AdminOrders}.tsx, Frontend/src/locales/{ru,tj}/{admin,farmer}.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`). Живой прогон не делала — стоит открыть "Заказы" в обеих панелях у заказа с несколькими позициями и убедиться, что видна только первая строка + "ещё N", а клик по ссылке разворачивает остальные без скачков высоты всей таблицы.

## 07:12 — "Ещё N" переехало на ту же строку — высота строк снова одинаковая

- Что сделано: пользователь прислала скриншот — вертикальный скролл страницы всё ещё оставался, потому что "ещё N" рисовалась ПОД первым товаром отдельной строкой (см. `gap-1 flex-col`), то есть заказы с несколькими товарами всё равно оставались на ~20px выше однопозиционных, и это накапливалось по всем видимым строкам. Убрала эту разницу: в свёрнутом виде "ещё N" теперь на той же строке, что и первый товар (`flex items-center gap-1.5`, не `flex-col`) — свёрнутая ячейка занимает ровно одну строку всегда, независимо от количества позиций в заказе. Разворачивание по клику (когда позиции реально нужно увидеть все) по-прежнему растягивает строку — это уже явное разовое действие пользователя, а не постоянное состояние таблицы.
- Файлы: Frontend/src/components/ui/OrderItemsCell.tsx
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`). Живой прогон не делала — полностью убедиться, что скролла совсем нет на конкретном окне пользователя, можно только вживую (высота окна браузера мне не видна); эта правка возвращает высоту строк ровно к тому, что было до добавления колонки "Товары".

## 07:16 — Формат даты+времени стал компактным числовым (не переносится на 3 строки)

- Что сделано: скролл всё ещё оставался — пользователь прислала скриншот, где "27 июля 2026, 06:31" переносится на 3 строки ("27" / "июля" / "2026, 06:31") в узких колонках "Дата и время заказа"/"Дата и время получения", раздувая и высоту строк, и общую ширину таблицы (горизонтальный скролл тоже был виден). Сама предложила: переписать формат даты компактнее.

  `formatDateTime()` в `lib/utils.ts` — было "27 июля 2026, 06:31" (полное название месяца текстом, зависит от перевода), стало "27.07.2026, 06:31" (числовой формат, помещается в одну строку в любой узкой колонке, и одинаков для ru/tj без завязки на перевод названий месяцев). Функция используется в 4 местах разом — во всех трёх таблицах заказов (Customer/Farmer/Admin) и в чате (пузыри сообщений, список переписок) — поправилось везде одним изменением. `formatDate()` (без времени, для дат сбора урожая и т.п., где места достаточно) не трогала — там полное название месяца по-прежнему уместно и красивее.
- Файлы: Frontend/src/lib/utils.ts
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`). Бэкенд не трогала (пользователь явно попросила больше не менять бэкенд) — правка целиком фронтовая, форматирование строки на клиенте. Живой прогон не делала — стоит открыть "Заказы" и проверить, что дата теперь в одну строку и горизонтальный/вертикальный скролл заметно уменьшился или пропал.

## 07:39 — Мобильный сайдбар для всех трёх панелей + таблицы "Заказы" пересобраны без горизонтального скролла

- Что сделано: пользователь попросила пройтись везде, где есть скролл, убрать его, отдельно сделать удобным на телефонах, и особенно — горизонтальный скролл влево-вправо в "Заказы" у фермера и админа. Явно попросила не трогать бэкенд. Запустила фоновый аудит (Explore-агент) по всем таблицам Admin/Farmer/Customer, чтобы понять реальный масштаб: 15 страниц с `overflow-x-auto`-таблицами, из них высокого риска — именно 3 страницы заказов (8-9 колонок), и отдельно — ни у одной из трёх панелей (`AdminLayout`/`FarmerLayout`/`CustomerLayout`) вообще не было мобильной адаптации: сайдбар фиксированной ширины (256/80px) рендерился всегда, без гамбургер-меню, без скрытия на узких экранах — то есть на телефоне сайдбар сам по себе съедал весь полезный экран.

  **1. Мобильный сайдбар (новое, влияет на все страницы разом).** Новый `components/layout/PanelMobileDrawer.tsx` — портал+бэкдроп+выезжающая слева панель (тот же приём, что уже был у публичного `MobileMenu.tsx`, только зеркально, слева, раз сайдбар панелей и так слева). В `AdminLayout.tsx`/`FarmerLayout.tsx`/`CustomerLayout.tsx`: постоянный `<aside>` теперь `hidden lg:flex` (скрыт на телефонах/планшетах), в хедере появилась кнопка-гамбургер `lg:hidden`, открывающая дровер с тем же списком меню (список пунктов вынесен в локальную функцию `navList(collapsed)`, чтобы не дублировать бейдж-логику между постоянным сайдбаром и дровером). Дровер сам закрывается при переходе на другую страницу.

  **2. Таблицы "Заказы" (Admin/Farmer/Customer) пересобраны в двух видах — таблица для десктопа/планшета (`hidden lg:block`) и карточки для телефона (`lg:hidden`), карточки вертикальные, горизонтальный скролл им физически не нужен.** На десктопе заодно сжала число колонок, объединяя связанные поля в одну ячейку (несколько строк вместо отдельной колонки на каждую), чтобы таблица помещалась без скролла даже на типичном ноутбучном экране:
  - `FarmerOrders.tsx`: было 9 колонок → стало 6 (№ заказа теперь с датой заказа внутри одной ячейки; статус — со статусом доставки и датой получения внутри одной ячейки).
  - `AdminOrders.tsx`: было 9 → стало 6 (то же самое + "Клиент"/"Фермер" объединены в одну колонку "Участники", две строки).
  - `CustomerOrders.tsx`: было 7 → стало 5 (то же объединение № заказа+дата, статус+получение).
- Файлы: Frontend/src/components/layout/{PanelMobileDrawer(новый),AdminLayout,FarmerLayout,CustomerLayout}.tsx, Frontend/src/pages/{FarmerOrders,AdminOrders,CustomerOrders}.tsx, Frontend/src/locales/{ru,tj}/admin.json
- Решения/примечания: `tsc -b --noEmit`, `vite build`, `oxlint` — чисто (та же единственная предсуществующая ошибка `AdminSettings.tsx`). Бэкенд не трогала вообще, как и попросили. **Не успела/не стала (пока)** переделывать под тот же карточный принцип остальные ~12 таблиц (Товары, Отзывы, Фермеры, Курьеры, Зоны доставки, Покупатели, Комиссии, Настройки, Документы, Сотрудники) — по аудиту большинство из них риска ниже (4-6 колонок, без многострочных ячеек), а мобильный сайдбар теперь освобождает им куда больше места, но на настоящем телефоне (не только на суженном окне) таблица с 4+ колонками физически всё ещё может потребовать горизонтальный скролл, раз карточной версии там нет. Сообщаю честно: если нужно то же самое (без скролла даже на телефоне) — везде, а не только в "Заказы", скажи, доделаю по той же схеме. Живой прогон не делала — стоит открыть панели на разных ширинах экрана (включая настоящий телефон/эмуляцию в DevTools) и проверить, что гамбургер открывает меню, а "Заказы" нигде не скроллятся вбок.
