**/bin/
**/obj/

# Локальные секреты — никогда не должны попасть в образ. В контейнере
# конфигурация приходит через переменные окружения (docker-compose.yml).
**/appsettings.json
**/appsettings.*.json

**/.vs/
**/.idea/
**/*.user
