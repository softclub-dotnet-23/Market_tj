## 12:37 — "Email уже существует" теперь всплывает сразу на первом шаге регистрации
- Что сделано: Раньше проверка "email уже зарегистрирован" была только в самом конце RegisterAsync — пользователь проходил шаг с email/телефоном, получал и вводил настоящий код с почты, заполнял регион/пароль и т.д., и только на финальном submit узнавал, что email занят. Добавила новый метод AuthService.SendRegistrationVerificationCodeAsync — та же проверка (GetByEmailAsync), что и в RegisterAsync, но выполняется ДО отправки кода. Endpoint POST /api/auth/send-verification-code (шаг 1 "Далее" в форме регистрации) теперь сразу отвечает 409 Conflict, если email занят, и не отправляет письмо с кодом впустую.
- Файлы: backend/MarketTJ.Application/Interfaces/Services/IAuthService.cs, backend/MarketTJ.Application/Services/AuthService.cs, backend/MarketTJ.WebApi/Controllers/AuthController.cs, backend/MarketTJ.Application.Tests/Services/AuthServiceTests.cs (+3 теста)
- Решения/примечания: Фронтенд (Register.tsx) уже показывает ошибку API через toast на этом же шаге ("Далее") — доработка чисто backend, менять React-код не пришлось. 837 тестов проходят.

## 13:12 — Admin "Настройки" (создание/удаление) и загрузка документов фермера заработали
- Что сделано:
  1. Проверила backend на все три раздела, о которых спросила пользователь (Настройки админа, Документы фермера, Сотрудники фермера) — оказалось, все три уже имеют полноценный backend (entity+repository+service+controller+тесты), просто фронтенд не везде использовал все возможности API.
  2. Admin "Настройки": страница только редактировала уже существующие записи — добавить новую было неоткуда (таблица была пустой, отсюда "Пока нет настроек"). Добавила кнопку "Добавить настройку" + модалку создания + удаление, используя уже готовые POST/DELETE /api/app-settings.
  3. Farmer "Документы": раньше это была таблица только для просмотра — загрузка была честно помечена как "появится после добавления файлового API" (такого API не было). Добавила POST /api/farmer-documents/upload (multipart, как у фото товаров/аватара) — валидация формата (jpg/png/webp/pdf) и размера (до 10 МБ) на бэкенде, новый документ создаётся со статусом "На проверке". На фронтенде — выбор типа документа + кнопка загрузки + удаление своих документов.
- Файлы: backend/MarketTJ.Application/{Interfaces/Services/IFarmerDocumentService.cs, Services/FarmerDocumentService.cs, Validators/FileUploadValidator.cs}, backend/MarketTJ.WebApi/{Controllers/FarmerDocumentController.cs, Models/UploadFarmerDocumentRequest.cs}, backend/MarketTJ.Application.Tests/Services/FarmerDocumentServiceTests.cs (+4 теста), Frontend/src/pages/{AdminSettings.tsx, FarmerDocuments.tsx}, Frontend/src/data/{adminEntities.ts, farmer.ts}, локали admin.json/farmer.json (ru/tj/en)
- Решения/примечания: Farmer "Сотрудники" (Staff) пока НЕ трогала — там реальная архитектурная развилка (нужен способ найти существующего пользователя по email/телефону, чтобы привязать его как сотрудника, а прямой поиск пользователей — это Admin-only функциональность и потенциальная утечка "зарегистрирован ли этот email", что противоречит уже принятому в проекте anti-enumeration подходу в Login/ForgotPassword). Вынесла вопрос пользователю отдельно, не стала решать самовольно. 841 тест проходит.

## 13:46 — Добавление сотрудника фермера по email (с уведомлением на почту)
- Что сделано: Реализован выбранный пользователем вариант — фермер вводит email будущего сотрудника; если аккаунт с таким email существует и ещё не привязан как сотрудник — сразу создаётся запись FarmerStaffMember, и сотруднику отправляется письмо на почту с уведомлением. Если email не зарегистрирован ИЛИ уже привязан как сотрудник — наружу возвращается одна и та же обобщённая ошибка "не удалось добавить, проверьте email", без уточнения причины (тот же anti-enumeration принцип, что уже используется в AuthService.LoginAsync). Сбой отправки письма не откатывает добавление сотрудника — только логируется предупреждением.
- Файлы: backend/MarketTJ.Application/{Dto/FarmerStaffMemberDto/CreateFarmerStaffMemberByEmailDto.cs (новый), Interfaces/Services/IFarmerStaffMemberService.cs, Services/FarmerStaffMemberService.cs}, backend/MarketTJ.WebApi/Controllers/FarmerStaffMemberController.cs, backend/MarketTJ.Application.Tests/Services/FarmerStaffMemberServiceTests.cs (+6 тестов), Frontend/src/pages/FarmerStaff.tsx, Frontend/src/data/farmer.ts, локали farmer.json (ru/tj/en)
- Решения/примечания: 847 тестов проходит. Также добавлено удаление сотрудника (уже было готово на бэкенде, просто не было кнопки на фронтенде).
