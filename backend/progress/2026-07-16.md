## 13:54 — Domain-схема переведена на прямые FK к User, добавлены repositories/Redis/EF-миграция

- Что сделано:
  - Схема пересобрана по новому детальному ТЗ пользователя: `CartItem/Order/Review` теперь ссылаются на `User` напрямую (`CustomerId`/`FarmerId` → `User.Id`), а не на `CustomerProfile`/`FarmerProfile`; `User.Role` — enum `UserRole`; `CustomerProfile.CustomerType` — enum `CustomerType`; `FarmerVerificationStatus` (`Pending/Verified/Rejected`) и `ListingStatus` (`Draft/Active/OutOfStock/Archived`) обновлены под новые значения; `Delivery.CourierId`/`AssignedAt` стали nullable; soft delete (`IsDeleted`/`DeletedAt`) добавлен в `User`, `ProductListing`, `Order`.
  - Репозитории переписаны в стиле пользователя (узкие интерфейсы на сущность, без общего `IRepository<T>`, `AddAsync/UpdateAsync/DeleteAsync` сами вызывают `SaveChangesAsync`) — 14 интерфейсов в `Application/Interfaces/Repositories/`, 14 реализаций в `Infrastructure/Persistence/Repositories/`.
  - Добавлен `ICacheService`/`RedisCacheService` (Redis через `IDistributedCache`, без прямого `StackExchange.Redis` в Application) — используется в `CategoryRepository` и `ProductListingRepository` (кэш списка, явная инвалидация на Add/Update/Delete).
  - DI (`Infrastructure/DependencyInjection.cs`) регистрирует `AppDbContext`, Redis, `ICacheService`, все 14 репозиториев.
  - Сгенерирована миграция `InitialCreate` (`dotnet ef migrations add`) — без ошибок, есть некритичные warning про global query filter (soft delete) на обязательных связях с `User`/`Order`/`ProductListing` — ожидаемо, т.к. soft delete есть только у этих трёх сущностей.
- Файлы: см. `git show --stat` коммита "Phase 1 Step 1 [BE]: domain entities + EF configurations + repositories".
- Решения/примечания:
  - Раздела 11.2.2 не было в старой версии ТЗ — теперь есть в новой (`TZ_MarketTJ_ClaudeCode1.md`), сверился с ним для стиля репозиториев.
  - **Не реализовано**: 16 дополнительных сущностей из раздела 8 нового ТЗ (Conversation, ChatMessage, Payment, RefundRequest, Commission, SupportTicket и др.) — включая Payment/RefundRequest, которые прямо противоречат разделу 3 того же файла («В MVP не входят: онлайн-оплата, настоящий escrow»). Не стал реализовывать самостоятельно из-за явного денежного/scope-конфликта — уточняю у пользователя отдельным вопросом.

## 14:24 — Добавлены все 16 сущностей раздела 8 (30 всего), CartItem/Order/Review откачены на Profile-based FK

- Что сделано:
  - Пользователь подтвердил: реализовать все 30 сущностей сейчас (несмотря на конфликт Payment/RefundRequest с разделом 3 — зафиксировано как явное решение пользователя, не моя инициатива).
  - **Откат**: `CartItem.Customer`, `Order.Customer/Farmer`, `Review.Customer/Farmer` возвращены на `CustomerProfile`/`FarmerProfile` (не `User`) — раздел 9 `TZ_MarketTJ_ClaudeCode1.md` явно требует связь через профили; пользователь подтвердил это отдельным вопросом. `User`/`CustomerProfile`/`FarmerProfile` entity и их EF-конфигурации обновлены обратно.
  - Добавлено 16 новых сущностей (раздел 8.15–8.30): Conversation, ChatMessage, AppSetting, FarmerDocument, AuditLog, ReportedListing, RefundRequest, DeliveryZone, Commission, Payment, Favorite, FarmerStaffMember, SupportTicket, SupportMessage, DeliverySlot, DailySalesSnapshot — с EF-конфигурациями, репозиториями (интерфейс+реализация, в том же стиле) и DbSet в `AppDbContext`.
  - Добавлено 9 новых enum: `FarmerDocumentType`, `DocumentReviewStatus`, `ReportReason`, `ReportStatus`, `RefundStatus`, `PaymentMethod`, `PaymentStatus`, `StaffPermissions` (`[Flags]`), `SupportTicketStatus`, `SupportPriority`.
  - Миграция `InitialCreate` удалена и пересоздана заново под финальную 30-сущностную модель (старая миграция никогда не применялась к реальной БД, поэтому просто перегенерирована, а не добавлена вторым шагом).
  - `dotnet build` — 0 ошибок, 0 warning. Миграция сгенерирована без ошибок (только некритичные warning про global query filter, как и раньше).
- Файлы: 16 entity, 16 EF-конфигураций, 16 интерфейсов репозиториев, 16 реализаций, 9 enum, обновлённые `User.cs`/`CustomerProfile.cs`/`FarmerProfile.cs`/`Category.cs`/`ProductListing.cs`/`AppDbContext.cs`/`DependencyInjection.cs`, новая миграция `InitialCreate`.
- Решения/примечания:
  - `DeliveryZone` — `Delivery` связь (раздел 9: «через совпадение Region/District, необязательный FK») **не реализована как реальный FK**: в разделе 8.12 (список полей `Delivery`) нет поля `DeliveryZoneId`. Оставил `Delivery` без изменений, `DeliveryZone` — без обратной коллекции. Если нужен настоящий FK — надо явно добавить поле, сам не стал додумывать.
  - `Conversation.CustomerId/FarmerId` и `RefundRequest.CustomerId`, `Favorite.CustomerId` и т.д. — оставлены на `User` напрямую (раздел 8.15/8.21/8.25 явно так говорят, раздел 9 не требует для них связи через профили, в отличие от CartItem/Order/Review).
  - `RefundRequest`: «не может быть двух Pending одновременно» — реализовано частичным уникальным индексом (`WHERE "Status" = 1`, PostgreSQL-специфичный синтаксис).
  - Реверс-коллекции на `User` добавлены не для всех admin-полей (`ReviewedByAdminId`, `ProcessedByAdminId`, `AssignedToAdminId` и т.п.) — иначе `User` разросся бы до ~15 коллекций ради полей, которые нужны редко. Если понадобится — можно добавить точечно.
