
## 11:35 — Category: обязательные переводы на 3 языка (Name/NameTj/NameEn)
- Что сделано: пользователь попросила, чтобы при добавлении категории в админке сразу вводились названия на всех 3 языках (для корректной работы во время фильтрации/отображения у покупателя на разных языках). Раньше Category.Name был одной нелокализованной строкой — фронтенд переводил только 6 изначально засеянных категорий через захардкоженный словарь в locales/data.json, для новых категорий (которые до сегодняшнего дня вообще нельзя было добавить — см. запись 11:05) перевода не было бы вовсе. Добавила Category.NameTj/NameEn (nullable-колонки, миграция AddCategoryTranslations, с бэкфиллом переводов для существующих 6 категорий), сделала оба поля обязательными в CategoryValidator при создании/редактировании. Форма в /admin/catalog теперь требует все 3 названия. Фронтенд (data/categories.ts) выбирает нужное поле по текущему i18n.language вместо старого словаря-хака.
- Файлы: backend/MarketTJ.Domain/Entities/Category.cs, backend/MarketTJ.Application/{Dto/CategoryDto/*,Validators/CategoryValidator.cs,Services/CategoryService.cs}, backend/MarketTJ.Infrastructure/Migrations/20260730055419_AddCategoryTranslations.cs(+.Designer.cs), backend/MarketTJ.Application.Tests/Services/CategoryServiceTests.cs, Frontend/src/data/{adminEntities.ts,categories.ts,catalogStore.ts}, Frontend/src/pages/AdminCatalog.tsx, Frontend/src/types/index.ts, локали admin.json (ru/tj/en)
- Решения/примечания: Description категории оставила одноязычным (как и раньше) — просили только название, под фильтрацию именно оно и влияет (Catalog.tsx фильтрует по числовому categoryId, не по тексту, так что сама фильтрация и раньше работала корректно — проблема была именно в отображении текста не на том языке). Миграция применена к локальной БД, бэкенд тесты 859/859 зелёные. Пришлось на секунду остановить её запущенный backend (dotnet run), чтобы сгенерировать миграцию (файлы были залочены) — нужно перезапустить backend.

## 15:20 — Категория за админом, название и единица измерения — за фермером
- Что сделано: по прямому запросу пользователя изменена модель создания объявления. Раньше фермер выбирал готовый "Товар" (Помидор/Картофель и т.д.) из справочника, который вёл админ — теперь фермер сам вводит название (уже было — поле "Название объявления") и выбирает категорию + единицу измерения (кг/шт/л/пучок/голова) напрямую. Добавила ProductListing.CategoryId (int) и ProductListing.Unit (string); ProductId стал необязательным (nullable) — оставлен только для уже существующих объявлений, ничего не удаляла из данных. Миграция AddCategoryAndUnitToProductListing с бэкфиллом (CategoryId/Unit взяты у связанного Product для уже существующих 16 объявлений) применена к базе.
- Файлы (backend): Domain/Entities/{ProductListing.cs,Category.cs}, Application/{Dto/ProductListingDto/*, Validators/ProductListingValidator.cs, Services/ProductListingService.cs}, Infrastructure/{Persistence/Configurations/ProductListingConfiguration.cs, Persistence/Repositories/ProductListingRepository.cs (null-safe SearchAsync для AI-ассистента), Persistence/Seed/Seeder.cs, Migrations/20260730090159_AddCategoryAndUnitToProductListing.cs}, Application.Tests/Services/ProductListingServiceTests.cs
- Файлы (frontend): data/{farmer.ts, catalogStore.ts (убрала join с /products, категория/единица теперь напрямую с объявления, фолбэк-фото теперь ищет по подстроке в Title вместо точного имени базового товара)}, pages/{FarmerProducts.tsx (два независимых select: Категория + Единица измерения, вместо каскада Категория→Товар), AdminCatalog.tsx (убрала вкладку "Товары" — она стала не нужна, оставила только управление категориями, переименовала пункт меню в "Категории")}, components/layout/AdminLayout.tsx, локали farmer.json/admin.json/product.json (ru/tj/en, добавила единицу "голова" для скота)
- Решения/примечания: backend Product-справочник (ProductController/ProductService) и связанные adminEntities.ts-функции (createCatalogProduct и т.д.) не удаляла — оставила как рабочий, но больше не используемый в этом сценарии код, чтобы не рисковать перед сдачей. Тесты бэкенда 860/860 зелёные. Проверила вживую через Playwright: создание объявления с категорией+единицей работает (тост "Маҳсулот илова карда шуд"), публичный каталог (/catalog) рендерится нормально с фото/ценами/категориями (15 товаров), новая страница "Категории" в админке работает без вкладок.

## 17:00 — Обращения в поддержку без регистрации + новая страница ответа для админа
- Что сделано: по прямому запросу пользователя ("чтобы без регистрации тоже могли писать, и админ мог ответить именно тому, кто задал вопрос"):
  1. Backend: ProductListing... нет, SupportTicket.UserId стал nullable (int?) + добавлены GuestName/GuestEmail; SupportMessage.SenderId тоже nullable (для самого первого сообщения гостя). Новый анонимный эндпоинт POST /api/support-tickets/guest создаёт тикет и первое сообщение одним вызовом ([AllowAnonymous], единственная анонимная точка во всей группе support-контроллеров). Когда Admin отвечает на любой тикет (гостевой или от зарегистрированного) — SupportMessageService теперь отправляет email с текстом ответа на GuestEmail (для гостя) или на email аккаунта (для зарегистрированного) через уже существующий IEmailSender/SMTP — единственный способ гостю вообще узнать об ответе, раз у него нет сессии.
  2. Frontend: Contact.tsx — убрала блок "Войдите, чтобы написать нам", форма теперь всегда видна (для гостя — только имя+email обязательны, телефон необязателен; для залогиненного имя/email подставляются автоматически). Новая страница админки "Поддержка" (/admin/support) — список обращений (с бейджем "Без регистрации" у гостевых), клик открывает переписку в виде чата (сообщения гостя слева, ответы админа справа, зелёные) с полем ответа.
  3. Миграция AddGuestSupportTicket применена к базе, тесты бэкенда 869/869 зелёные (добавила 9 новых: гостевое создание тикета + 4 теста на email-уведомление при ответе админа).
- Файлы (backend): Domain/Entities/{SupportTicket.cs,SupportMessage.cs}, Application/{Common/CurrentUserAuthorizationExtensions.cs, Dto/SupportTicketDto/*, Dto/SupportMessageDto/GetSupportMessageDto.cs, Validators/SupportTicketValidator.cs, Services/{SupportTicketService.cs,SupportMessageService.cs}, Interfaces/Services/ISupportTicketService.cs}, Infrastructure/{Persistence/Configurations/{SupportTicketConfiguration.cs,SupportMessageConfiguration.cs}, Persistence/Repositories/... (не трогала), Migrations/20260730104701_AddGuestSupportTicket.cs}, WebApi/Controllers/SupportTicketController.cs, Application.Tests/Services/{SupportTicketServiceTests.cs,SupportMessageServiceTests.cs}
- Файлы (frontend): pages/{Contact.tsx, AdminSupport.tsx (новый)}, data/{support.ts, adminSupport.ts (новый)}, App.tsx, components/layout/AdminLayout.tsx, локали admin.json/pages.json (ru/tj/en)
- Решения/примечания: проверила вживую полный цикл — гость отправил обращение с /contact без входа (тост "Сообщение отправлено", форма без блока логина), в /admin/support обращение появилось с пометкой "Без регистрации" и email гостя, ответ админа корректно ушёл (в логах бэкенда нет ошибок отправки — тот же SMTP, что уже работает для писем при регистрации). Управление статусом тикета (открыт/в работе/закрыт) не добавляла — не входило в прямой запрос, можно добавить отдельно при необходимости.

## 17:15 — Фикс: ответ в поддержке "завис" из-за таймаута SMTP в 100 секунд
- Что сделано: старый .NET SmtpClient (System.Net.Mail), который использует SmtpEmailSender, по умолчанию ждёт до 100 секунд перед тем как сдаться — и т.к. отправка email-уведомления происходит внутри того же запроса, что и сохранение ответа админа (см. запись 17:00), UI зависал на это время, хотя сам ответ уже сохранялся в базу почти сразу. Поставила Timeout = 10 секунд на SmtpClient — так и обычная отправка укладывается с запасом, и в худшем случае (SMTP недоступен) запрос быстро отдаёт ошибку в лог, не блокируя интерфейс надолго.
- Файлы: backend/MarketTJ.Infrastructure/Email/SmtpEmailSender.cs
- Решения/примечания: тот же SmtpEmailSender используется и для писем при регистрации — тоже выигрывает от фикса, не только поддержка.

## 19:04 — Фикс: логин падал из-за пропавшего appsettings.json после мержа PR
- Что сделано: после мержа Frontend/Backend веток на GitHub (коммит "Sexteen commit") файл backend/MarketTJ.WebApi/appsettings.json оказался физически удалён с диска, а .gitignore стал игнорить его целиком (раньше — только appsettings.*.json с суффиксом окружения). Из-за этого пропали некритичные, но обязательные для рантайма ключи (Jwt:Issuer/Audience/ExpiryMinutes, Cors:AllowedOrigins), которых нет в user-secrets (там только реальные секреты — пароль БД, Jwt:Secret, SMTP) — TokenService падал при выдаче токена на каждом логине, отсюда "Не удалось войти" в браузере. Восстановила appsettings.json по шаблону appsettings.json.example (пароль БД там — плейсхолдер, реальный берётся из user-secrets, которые важнее по порядку конфигурации). Проверила: backend поднимается, /health → 200, /api/auth/login отдаёт чистый 401 при неверном пароле (не 500) — конфигурация грузится корректно.
- Файлы: backend/MarketTJ.WebApi/appsettings.json (создан заново, гитигнорится — см. git check-ignore, в репозиторий не попадёт)
- Решения/примечания: appsettings.json.example — актуальный источник правды для локальной структуры конфига; при следующей пропаже файла (например, после чистого клона) достаточно скопировать его и убедиться, что dotnet user-secrets содержит ConnectionStrings:DefaultConnection/Jwt:Secret/Smtp:*.

## 19:13 — Admin "Покупатели": добавлены редактирование и удаление
- Что сделано: по прямому запросу пользователя добавила полноценный CRUD на странице /admin/users — раньше была только таблица на чтение. Backend уже поддерживал общий Admin-only CRUD (GET/POST/PUT/DELETE /api/users, любая роль) — фронтенд просто не использовал Create/Update/Delete для покупателей. Добавила кнопку "Добавить покупателя", модалку создания/редактирования (ФИО, email, телефон, пароль — необязателен при редактировании, "оставьте пустым, чтобы не менять"), значки редактировать/удалить в таблице и в карточках, диалог подтверждения удаления.
- Файлы: Frontend/src/pages/AdminUsers.tsx (переписан), Frontend/src/data/adminEntities.ts (useAdminCustomers получил refreshKey, добавлены createCustomer/updateCustomer/deleteCustomer — роль всегда фиксируется как Customer), локали admin.json (ru/tj/en, секция users)
- Решения/примечания: роль при создании/редактировании не выставляется в форме — всегда Customer, т.к. это страница именно покупателей (смена роли пользователя — отдельная админская операция, ChangeRole, не входила в запрос). Проверила вживую через Playwright полный цикл create → edit → delete — все тосты и confirm-диалог отработали корректно, tsc чистый.

## 19:40 — Фикс: удаление покупателя навсегда блокировало повторную регистрацию с тем же email/телефоном
- Что сделано: после того как только что добавленную кнопку "Удалить" в /admin/users опробовали на реальном покупателе (Nekruz Rashidov) и тут же попытались зарегистрировать того же человека заново — регистрация падала с generic "Не удалось зарегистрироваться". Раскопала через прямой запрос к БД (Users.IsDeleted=true, но письмо-код подтверждения новый и подтверждён, значит падало именно на INSERT): удаление — мягкое (User.IsDeleted=true), а уникальные индексы на Users.Email/Users.PhoneNumber в БД были безусловными, без учёта IsDeleted. Логика "email/телефон свободен" (GetByEmailAsync и т.д.) использует EF query filter (!IsDeleted) и потому видит их как свободные — а сам INSERT в БД падает на констрейнте, который про мягкое удаление не знает; исключение ловится общим catch в AuthService.RegisterAsync и превращается в неинформативное сообщение.
- Файлы: backend/MarketTJ.Infrastructure/Persistence/Configurations/UserConfiguration.cs (индексы теперь partial: `HasFilter("\"IsDeleted\" = false")`), Migrations/20260730142439_FilterSoftDeletedUsersFromUniqueIndex.cs(+.Designer.cs)
- Решения/примечания: проверила фикс напрямую — воспроизвела её точный сценарий (email nekruzrashidov@gmail.com, тот же телефон) через API уже с новой миграцией — регистрация прошла успешно (200 OK, новый userId). Тестового пользователя, созданного только для проверки, удалила из БД, чтобы не занимать email/телефон перед её собственной повторной попыткой через реальный UI. Проверила остальные Unique-индексы в проекте (CustomerProfile/FarmerProfile/CourierProfile.UserId и т.д.) — там такой проблемы нет, т.к. они привязаны к конкретному Id пользователя, а не переиспользуемому email/телефону. Backend-тесты 889/889 зелёные. Нужно перезапустить backend — миграция применится автоматически при старте.
